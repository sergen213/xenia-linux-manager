---
phase: 04-library-review-and-launch-core
plan: 02
type: execute
wave: 2
depends_on: [01]
files_modified:
  - src-tauri/src/library/identity.rs
  - src-tauri/src/library/review.rs
  - src-tauri/src/commands/library.rs
  - src-tauri/src/lib.rs
  - src/features/library/api/libraryClient.ts
  - src/features/library/model/libraryTypes.ts
  - src/features/library/state/libraryStore.ts
  - src/features/library/components/GameIdentityEditor.tsx
  - src/features/library/components/ManualGameForm.tsx
  - src/features/library/components/DuplicateResolutionTable.tsx
  - src/features/library/components/GameDetailsPanel.tsx
  - src/features/library/components/ReviewInboxPanel.tsx
  - src/features/library/LibraryPage.tsx
autonomous: true
requirements: [LIB-04, LIB-05]
user_setup: []
must_haves:
    truths:
      - Users can manually add a game with only title and executable path, and the resulting manual entry appears as a first-class library item while remaining visually distinguishable as manually managed
      - Manual corrections and duplicate-resolution decisions are persisted in a backend identity overlay that always wins over conflicting future scan data
      - Duplicate review supports primary-and-alternates, merge, dismiss-false-duplicate, and leave-flagged outcomes from both queue-style review flow and sortable table navigation
    artifacts:
      - path: src-tauri/src/library/identity.rs
        provides: Persisted canonical game identity, manual override, duplicate-resolution, and review-status model layered on top of scan evidence
        min_lines: 130
      - path: src/features/library/components/GameIdentityEditor.tsx
        provides: In-detail correction workflow for title/path edits, issue notes, and override persistence without leaving the detail screen
        min_lines: 90
      - path: src/features/library/components/DuplicateResolutionTable.tsx
        provides: Sortable duplicate-review surface supporting the full set of review outcomes required by the phase context
        min_lines: 90
      - path: src/features/library/state/libraryStore.ts
        provides: Renderer actions for manual add, correction save, duplicate resolution, and review progression
        min_lines: 110
    key_links:
      - from: src-tauri/src/commands/library.rs
        to: src-tauri/src/library/identity.rs
        via: Library commands persist manual entries, override edits, and duplicate-resolution outcomes in backend-owned identity documents
        pattern: manual|override|duplicate|resolve
      - from: src-tauri/src/library/review.rs
        to: src-tauri/src/library/identity.rs
        via: Resolved browse/detail payloads apply canonical identity overlays and prefer manual decisions over scan evidence
        pattern: canonical|override|resolved
      - from: src/features/library/components/GameDetailsPanel.tsx
        to: src/features/library/components/GameIdentityEditor.tsx
        via: Manual correction happens inside the detail-first workflow rather than a separate detached screen
        pattern: edit|manual|identity
      - from: src/features/library/components/ReviewInboxPanel.tsx
        to: src/features/library/components/DuplicateResolutionTable.tsx
        via: Review queue and sortable duplicate table operate on the same persisted review-state contract
        pattern: review|duplicate|queue
---

<objective>
Implement manual add/correction and identity resolution persistence.

Purpose: Turn the browse shell into a real library-management workflow where users can fix bad detections, add missing titles, and resolve duplicates without those decisions being erased by later scans.
Output: Backend identity overlay, manual-entry and correction commands, duplicate-resolution workflows, and in-detail editing surfaces wired into the library state.
</objective>

<execution_context>
@/home/sergeng/.codex/get-shit-done/workflows/execute-plan.md
@/home/sergeng/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-library-review-and-launch-core/04-CONTEXT.md
@.planning/phases/04-library-review-and-launch-core/04-RESEARCH.md
@.planning/phases/04-library-review-and-launch-core/04-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create the backend identity overlay for canonical games, manual entries, and review outcomes</name>
  <files>src-tauri/src/library/identity.rs, src-tauri/src/library/review.rs, src-tauri/src/commands/library.rs, src-tauri/src/lib.rs</files>
  <action>Implement a backend identity layer that stores canonical game ids, linked scan candidate ids, manual-only entries, manual title/path overrides, review state, and duplicate-resolution decisions separately from the Phase 3 scan catalog. The merge rules must always prefer explicit user edits over scan evidence and keep duplicate-resolution history stable across rescans.</action>
  <verify><automated>cargo test --manifest-path src-tauri/Cargo.toml library::identity</automated></verify>
  <done>The backend can persist and reload canonical library identity, manual entries, and duplicate outcomes without mutating raw scan evidence.</done>
</task>

<task type="auto">
  <name>Task 2: Add renderer actions for manual add, correction save, and review progression</name>
  <files>src/features/library/api/libraryClient.ts, src/features/library/model/libraryTypes.ts, src/features/library/state/libraryStore.ts, src/features/library/components/ManualGameForm.tsx, src/features/library/components/ReviewInboxPanel.tsx</files>
  <action>Extend the library client and store so the renderer can create manual entries, save title/path corrections, progress review queues, and reload resolved browse data after each decision. Manual add must stay intentionally lightweight with only title and executable path required, while review workflows must support queue progression and jumping around from a sortable table without losing in-progress context.</action>
  <verify><automated>npm run test -- --run src/features/library/state src/features/library/components</automated></verify>
  <done>The renderer can create and update canonical games through typed backend calls while keeping review workflow state coherent across queue and table modes.</done>
</task>

<task type="auto">
  <name>Task 3: Embed correction and duplicate-resolution UI directly into the detail-first library workflow</name>
  <files>src/features/library/components/GameIdentityEditor.tsx, src/features/library/components/DuplicateResolutionTable.tsx, src/features/library/components/GameDetailsPanel.tsx, src/features/library/components/ReviewInboxPanel.tsx, src/features/library/LibraryPage.tsx</files>
  <action>Build the user-facing correction experience inside the existing detail-first library flow rather than adding a detached modal-only editor. The detail view should expose inline title/path edits, manual-entry markers, and issue notes, while the review inbox should support the full set of duplicate-resolution outcomes: mark primary with alternates, merge into one logical game, dismiss false duplicates, or leave flagged for later.</action>
  <verify><automated>npm run test -- --run src/features/library</automated></verify>
  <done>Users can correct a game's identity from its detail view, add missing titles with a minimal form, and resolve duplicates through both queue and table review modes.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] cargo test --manifest-path src-tauri/Cargo.toml library::identity
- [ ] npm run test -- --run src/features/library/state src/features/library/components
- [ ] npm run test -- --run src/features/library
- [ ] npm run build
</verification>

<success_criteria>

- All tasks completed
- Manual add requires only title and executable path and produces a first-class library item
- Manual corrections persist in a backend identity overlay that overrides conflicting future scan data
- Duplicate review supports all required resolution outcomes from both queue-style and sortable-table workflows

</success_criteria>

<output>
After completion, create `.planning/phases/04-library-review-and-launch-core/04-02-SUMMARY.md`
</output>
