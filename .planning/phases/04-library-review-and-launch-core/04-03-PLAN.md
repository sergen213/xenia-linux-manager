---
phase: 04-library-review-and-launch-core
plan: 03
type: execute
wave: 3
depends_on: [01, 02]
files_modified:
  - src-tauri/src/library/launch.rs
  - src-tauri/src/library/review.rs
  - src-tauri/src/commands/library.rs
  - src-tauri/src/commands/xenia.rs
  - src-tauri/src/lib.rs
  - src/features/library/api/libraryClient.ts
  - src/features/library/model/libraryTypes.ts
  - src/features/library/state/libraryStore.ts
  - src/features/library/components/LaunchPreflightPanel.tsx
  - src/features/library/components/LaunchWarningDialog.tsx
  - src/features/library/components/GameDetailsPanel.tsx
  - src/features/library/LibraryPage.tsx
  - src/features/dashboard/DashboardHome.tsx
autonomous: true
requirements: [LAUN-01, LAUN-03, LIB-05]
user_setup: []
must_haves:
    truths:
      - Launch preflight is backend-owned and hard-blocks when Xenia is missing, the executable path is absent or missing on disk, review or duplicate state is unresolved, or the selected source shape is unsupported
      - Suspicious but technically launchable titles surface explicit warnings with user choice to continue, and the game detail view exposes both inline recovery actions and a modal warning flow
      - A successful launch request spawns the installed Linux Xenia build, records running-session metadata immediately, and updates last-played state so the library and dashboard reflect the start of play
    artifacts:
      - path: src-tauri/src/library/launch.rs
        provides: Backend preflight classification, Xenia process argument construction, shell-based process spawn, and session-state persistence
        min_lines: 130
      - path: src/features/library/components/LaunchPreflightPanel.tsx
        provides: Detail-view launch readiness UI with blocking reasons, warning summaries, and recovery actions
        min_lines: 90
      - path: src/features/library/state/libraryStore.ts
        provides: Renderer launch action wiring, preflight refresh, warning-confirmation handling, and last-played/session updates
        min_lines: 110
      - path: src/features/library/components/LaunchWarningDialog.tsx
        provides: Suspicious-but-launchable confirmation flow that explains risks before the user proceeds
        min_lines: 70
    key_links:
      - from: src-tauri/src/library/launch.rs
        to: src-tauri/src/commands/xenia.rs
        via: Launch preflight reads persisted Xenia install state rather than trusting renderer assumptions about emulator readiness
        pattern: install|status|xenia
      - from: src-tauri/src/commands/library.rs
        to: src-tauri/src/library/launch.rs
        via: Library launch commands expose structured preflight results and launch execution from the backend to the detail view
        pattern: launch|preflight|warning|block
      - from: src/features/library/components/GameDetailsPanel.tsx
        to: src/features/library/components/LaunchPreflightPanel.tsx
        via: Game detail remains the single control surface for launch, recovery actions, and readiness messaging
        pattern: launch|ready|recovery
      - from: src/features/library/state/libraryStore.ts
        to: src/features/dashboard/DashboardHome.tsx
        via: Successful launches propagate running-session and last-played data back into high-level library activity summaries
        pattern: running|last|played
---

<objective>
Add launch preflight validation and Xenia process spawning.

Purpose: Complete Phase 4 by turning curated game records into safely launchable titles, with backend-owned readiness checks, clear recovery guidance, and immediate session tracking when launch succeeds.
Output: Backend preflight and launch module, renderer launch state/actions, detail-view readiness UI, warning-confirmation flow, and last-played/session updates across library surfaces.
</objective>

<execution_context>
@/home/sergeng/.codex/get-shit-done/workflows/execute-plan.md
@/home/sergeng/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-library-review-and-launch-core/04-CONTEXT.md
@.planning/phases/04-library-review-and-launch-core/04-RESEARCH.md
@.planning/phases/04-library-review-and-launch-core/04-01-SUMMARY.md
@.planning/phases/04-library-review-and-launch-core/04-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build backend launch preflight and Xenia process-spawn contracts</name>
  <files>src-tauri/src/library/launch.rs, src-tauri/src/library/review.rs, src-tauri/src/commands/library.rs, src-tauri/src/commands/xenia.rs, src-tauri/src/lib.rs</files>
  <action>Create a backend launch module that loads the resolved game identity, validates launch prerequisites against the installed Xenia state, classifies outcomes into blocking errors, warnings, or ready-to-launch status, and spawns the Linux Xenia process through the existing shell plugin when launch is allowed. Hard-block the exact invalid states required by context, and persist running-session plus `last_played_at` metadata immediately after a successful spawn request.</action>
  <verify><automated>cargo test --manifest-path src-tauri/Cargo.toml library::launch</automated></verify>
  <done>The backend can explain why a game cannot launch, warn when it is suspicious but technically launchable, and start Xenia with a resolved game path when the user proceeds.</done>
</task>

<task type="auto">
  <name>Task 2: Add renderer launch actions, warning-confirmation flow, and session updates</name>
  <files>src/features/library/api/libraryClient.ts, src/features/library/model/libraryTypes.ts, src/features/library/state/libraryStore.ts, src/features/library/components/LaunchWarningDialog.tsx</files>
  <action>Extend the library client and store to request preflight results, preserve structured blocking or warning details, confirm suspicious launches explicitly, and refresh browse/detail state after launch updates running-session or `last_played_at` metadata. Keep launch state inside the library feature rather than inventing a detached launcher subsystem.</action>
  <verify><automated>npm run test -- --run src/features/library/state src/features/library/components</automated></verify>
  <done>The renderer can request preflight, show warning confirmation when required, launch through the backend contract, and keep library state aligned with session changes.</done>
</task>

<task type="auto">
  <name>Task 3: Add detail-view preflight messaging, recovery actions, and launch-driven library updates</name>
  <files>src/features/library/components/LaunchPreflightPanel.tsx, src/features/library/components/GameDetailsPanel.tsx, src/features/library/LibraryPage.tsx, src/features/dashboard/DashboardHome.tsx</files>
  <action>Finish the detail-first launch experience with inline blocking-reason presentation, actionable recovery steps, and warning explanations that make suspicious launches understandable. After successful launch, confirm that the request started, reflect running-session state in the detail view, and update last-played visibility in both the library grid and dashboard activity surfaces.</action>
  <verify><automated>npm run test -- --run src/features/library src/features/dashboard</automated></verify>
  <done>Users can understand launch readiness from the game detail view, fix blocking issues when possible, and see immediate confirmation plus updated library activity when launch starts.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] cargo test --manifest-path src-tauri/Cargo.toml library::launch
- [ ] npm run test -- --run src/features/library/state src/features/library/components
- [ ] npm run test -- --run src/features/library src/features/dashboard
- [ ] npm run build
</verification>

<success_criteria>

- All tasks completed
- Backend-owned preflight hard-blocks invalid launch states and distinguishes them from warnings
- Suspicious-but-launchable titles require explicit confirmation with clear reasons
- Successful launch spawns the installed Xenia build and immediately updates running-session and last-played library state

</success_criteria>

<output>
After completion, create `.planning/phases/04-library-review-and-launch-core/04-03-SUMMARY.md`
</output>
