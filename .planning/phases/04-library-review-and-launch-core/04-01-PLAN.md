---
phase: 04-library-review-and-launch-core
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src-tauri/src/library/review.rs
  - src-tauri/src/commands/library.rs
  - src-tauri/src/lib.rs
  - src/features/library/api/libraryClient.ts
  - src/features/library/model/libraryTypes.ts
  - src/features/library/state/libraryStore.ts
  - src/features/library/state/LibraryProvider.tsx
  - src/features/library/components/LibraryFiltersBar.tsx
  - src/features/library/components/LibraryGrid.tsx
  - src/features/library/components/ReviewInboxPanel.tsx
  - src/features/library/components/GameDetailsPanel.tsx
  - src/features/library/LibraryPage.tsx
  - src/features/dashboard/DashboardHome.tsx
  - src/app/router.tsx
autonomous: true
requirements: [LIB-05]
user_setup: []
must_haves:
    truths:
      - The main library experience renders a cover-grid of resolved games with title, last-played time, and relevant duplicate or review badges instead of exposing raw scan candidates directly
      - Selecting a game opens a dedicated detail-first experience that shows identity, executable path, source evidence, scan confidence/history, artwork when available, and issue notes
      - Duplicate and low-confidence review work is visible in a separate review inbox tab with both queue-oriented and sortable browsing entry points, not mixed into the primary library grid
    artifacts:
      - path: src-tauri/src/library/review.rs
        provides: Backend read model that merges scan catalog evidence into browse-ready cards, review inbox counts, and detailed game payloads
        min_lines: 110
      - path: src/features/library/state/libraryStore.ts
        provides: Renderer library browse state, detail selection, search/filter/sort actions, and review-inbox loading using the existing provider/reducer pattern
        min_lines: 100
      - path: src/features/library/components/LibraryGrid.tsx
        provides: Cover-grid library surface with status badges and selection into detail view
        min_lines: 90
      - path: src/features/library/components/GameDetailsPanel.tsx
        provides: Detail-first library view that exposes browse context, source evidence, and the shell for later correction and launch actions
        min_lines: 100
    key_links:
      - from: src/features/library/api/libraryClient.ts
        to: src-tauri/src/commands/library.rs
        via: Renderer invokes typed backend browse/detail/review commands rather than reconstructing library state locally
        pattern: library|review|detail|browse
      - from: src-tauri/src/commands/library.rs
        to: src-tauri/src/library/review.rs
        via: Library commands expose resolved browse cards, review inbox summaries, and detail payloads built from backend-owned scan evidence
        pattern: review|detail|grid|inbox
      - from: src/features/library/state/libraryStore.ts
        to: src/features/library/components/ReviewInboxPanel.tsx
        via: Review-tab state stays in the library provider and drives queue plus sortable inbox navigation
        pattern: review|queue|sort
      - from: src/features/library/components/LibraryGrid.tsx
        to: src/features/dashboard/DashboardHome.tsx
        via: Resolved library counts and recent-play metadata flow up to the dashboard so the app reports browseable library health
        pattern: detected|review|last
---

<objective>
Build the library UI, game detail views, and search/browse interactions.

Purpose: Turn the Phase 3 scan catalog into a resolved, browseable library experience that feels like an organizer and establishes the detail-first shell for correction and launch work.
Output: Backend browse/detail read model, typed renderer library client/store/provider, cover-grid library page, review inbox surfaces, and dedicated game detail navigation.
</objective>

<execution_context>
@/home/sergeng/.codex/get-shit-done/workflows/execute-plan.md
@/home/sergeng/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-library-review-and-launch-core/04-CONTEXT.md
@.planning/phases/04-library-review-and-launch-core/04-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create the backend browse and detail read model for the curated library</name>
  <files>src-tauri/src/library/review.rs, src-tauri/src/commands/library.rs, src-tauri/src/lib.rs</files>
  <action>Build a backend-owned read model that translates Phase 3 scan candidates into browse-ready library cards, review inbox summaries, and game detail payloads. The read model should preserve uncertainty explicitly, separate unresolved duplicate or low-confidence records from the main browse grid, and expose the exact evidence the detail view needs without forcing the renderer to understand raw catalog internals.</action>
  <verify><automated>cargo test --manifest-path src-tauri/Cargo.toml library::review</automated></verify>
  <done>The backend can answer `browse library`, `load review inbox`, and `load game details` requests with resolved payloads that match the phase context.</done>
</task>

<task type="auto">
  <name>Task 2: Add library browse state, search/filter/sort controls, and review inbox entry points</name>
  <files>src/features/library/api/libraryClient.ts, src/features/library/model/libraryTypes.ts, src/features/library/state/libraryStore.ts, src/features/library/state/LibraryProvider.tsx, src/features/library/components/LibraryFiltersBar.tsx, src/features/library/components/ReviewInboxPanel.tsx, src/features/library/LibraryPage.tsx</files>
  <action>Create the renderer state and client contracts for resolved browse cards, detail selection, and review inbox data using the same context + reducer architecture already used by settings and tasks. Add richer organizer controls for search, filter, and sort, and keep review work reachable from the library page through both a queue-style flow and a sortable-table inbox rather than burying it behind raw scan summaries.</action>
  <verify><automated>npm run test -- --run src/features/library/state src/features/library/components</automated></verify>
  <done>The library feature can load resolved browse data, switch between main library and review inbox views, and keep search/filter/sort state coherent without introducing renderer-only truth.</done>
</task>

<task type="auto">
  <name>Task 3: Build the cover-grid library and dedicated game detail view surfaces</name>
  <files>src/features/library/components/LibraryGrid.tsx, src/features/library/components/GameDetailsPanel.tsx, src/features/library/LibraryPage.tsx, src/features/dashboard/DashboardHome.tsx, src/app/router.tsx</files>
  <action>Replace the placeholder Library page with a cover-grid browse experience that shows title and last-played time by default, adds duplicate or review flags only when relevant, and routes selection into a dedicated detail-first view. The detail view must show identity, executable path, source folder, artwork when available, scan confidence/history, and issue notes so later correction and launch plans can extend the same screen rather than inventing a parallel flow.</action>
  <verify><automated>npm run test -- --run src/features/library src/features/dashboard src/app/router.test.tsx</automated></verify>
  <done>Users can browse a resolved library, open a dedicated detail view for a title, and clearly distinguish primary library browsing from unresolved review work.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] cargo test --manifest-path src-tauri/Cargo.toml library::review
- [ ] npm run test -- --run src/features/library/state src/features/library/components
- [ ] npm run test -- --run src/features/library src/features/dashboard src/app/router.test.tsx
- [ ] npm run build
</verification>

<success_criteria>

- All tasks completed
- The main library browse surface uses a cover grid with title and last-played metadata
- Selecting a game opens a dedicated detail-first view with source evidence and issue history
- Duplicate and low-confidence records are visible through a separate review inbox instead of being mixed into the primary library grid

</success_criteria>

<output>
After completion, create `.planning/phases/04-library-review-and-launch-core/04-01-SUMMARY.md`
</output>
