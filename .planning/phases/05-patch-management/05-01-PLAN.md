---
phase: 05-patch-management
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src-tauri/Cargo.toml
  - src-tauri/src/patches/mod.rs
  - src-tauri/src/patches/storage.rs
  - src-tauri/src/patches/sources.rs
  - src-tauri/src/patches/parser.rs
  - src-tauri/src/commands/patches.rs
  - src-tauri/src/lib.rs
  - src/features/library/api/libraryClient.ts
  - src/features/library/model/patchTypes.ts
  - src/features/library/state/libraryStore.ts
autonomous: true
requirements: [PATC-01, PATC-02]
user_setup: []
must_haves:
    truths:
      - Users can import a valid local `.patch.toml` file for a game, and the backend rejects unreadable or invalid patch files before they enter managed storage
      - The app can look up and fetch the matching community Canary patch file for a detected title, track remote provenance and last-fetched metadata, and require confirmation before replacing an already fetched remote copy
      - Imported and fetched patch files can coexist for the same game, with one explicit active patch file selection persisted independently from the patch source files
    artifacts:
      - path: src-tauri/src/patches/storage.rs
        provides: Backend patch inventory, manifest persistence, active-file selection, and entry-override storage rooted in library metadata instead of the Xenia install directory
        min_lines: 130
      - path: src-tauri/src/patches/sources.rs
        provides: Local-import and remote-community source adapters with validation, normalization, provenance tracking, and update-detection logic
        min_lines: 120
      - path: src-tauri/src/commands/patches.rs
        provides: Typed Tauri commands for listing patch files, importing local files, fetching remote patches, confirming updates, and selecting the active patch file
        min_lines: 90
      - path: src/features/library/state/libraryStore.ts
        provides: Renderer actions and reducer state for patch inventory loading, import/fetch progress, and active-patch selection inside the existing library workflow
        min_lines: 120
    key_links:
      - from: src-tauri/src/commands/patches.rs
        to: src-tauri/src/patches/storage.rs
        via: Renderer-facing patch commands persist canonical patch inventory and active-file selection in backend-owned metadata
        pattern: patch|active|storage|manifest
      - from: src-tauri/src/patches/sources.rs
        to: src-tauri/src/patches/storage.rs
        via: Imported and remote-fetched patch files are normalized into the same stored representation regardless of provenance
        pattern: import|remote|provenance|fetch
      - from: src/features/library/api/libraryClient.ts
        to: src-tauri/src/commands/patches.rs
        via: The library feature uses typed patch commands instead of reconstructing source or storage rules in the renderer
        pattern: patch|fetch|import|active
      - from: src/features/library/state/libraryStore.ts
        to: src/features/library/model/patchTypes.ts
        via: Patch inventory, active file metadata, and source-operation state are modeled explicitly in the existing provider and reducer architecture
        pattern: patch|source|active|status
---

<objective>
Implement patch source adapters and local patch storage model.

Purpose: Create the backend contract for Patch Management so the app can ingest local `.patch.toml` files, fetch supported remote community patches, and persist game-specific patch inventory without coupling patch state to the replaceable Xenia install directory.
Output: Patch storage and parsing modules, local and remote source adapters, typed patch commands, and renderer state hooks for inventory and active-file selection.
</objective>

<execution_context>
@/home/sergeng/.codex/get-shit-done/workflows/execute-plan.md
@/home/sergeng/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-patch-management/05-CONTEXT.md
@.planning/phases/05-patch-management/05-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build the backend patch inventory, parser, and persistence model</name>
  <files>src-tauri/Cargo.toml, src-tauri/src/patches/mod.rs, src-tauri/src/patches/storage.rs, src-tauri/src/patches/parser.rs</files>
  <action>Create a backend-owned patch domain rooted in `library_metadata_path` that stores installed patch files per canonical game, keeps source files separate from entry-level enabled-state overrides, and persists active patch selection independently from the underlying `.patch.toml` contents. The parser should normalize Xenia Canary patch files into editor-ready records and reject unreadable or invalid local patch files completely.</action>
  <verify><automated>cargo test --manifest-path src-tauri/Cargo.toml patches::storage</automated></verify>
  <done>The backend can persist patch inventory, normalize valid patch files, reject invalid files, and reload a game's patch state without relying on renderer-only assumptions.</done>
</task>

<task type="auto">
  <name>Task 2: Add local-import and remote-community source adapters with update detection</name>
  <files>src-tauri/src/patches/sources.rs, src-tauri/src/patches/storage.rs, src-tauri/src/patches/parser.rs</files>
  <action>Implement two source adapters: local `.patch.toml` import and remote community fetch from the Xenia Canary patch repository. Remote lookup must key off detected game identity rather than UI display text, store provenance and last-fetched metadata, and detect when a previously fetched remote patch has a newer version available so the UI can require explicit update confirmation before replacement.</action>
  <verify><automated>cargo test --manifest-path src-tauri/Cargo.toml patches::sources</automated></verify>
  <done>The backend can import a local patch file, fetch a matching remote community patch when supported, and distinguish fresh fetches from confirmed in-place remote updates.</done>
</task>

<task type="auto">
  <name>Task 3: Expose typed patch commands and library-state wiring for inventory and active-file selection</name>
  <files>src-tauri/src/commands/patches.rs, src-tauri/src/lib.rs, src/features/library/api/libraryClient.ts, src/features/library/model/patchTypes.ts, src/features/library/state/libraryStore.ts</files>
  <action>Expose typed Tauri commands for listing a game's managed patch files, importing or fetching new ones, confirming remote updates, and selecting the active patch file. Extend the library feature's existing client and reducer state so patch inventory, operation status, and active-file selection live inside the detail-first game workflow instead of a detached subsystem.</action>
  <verify><automated>npm run test -- --run src/features/library/state src/features/library/api</automated></verify>
  <done>The renderer can load managed patch inventory, start source operations, and persist the active patch file through typed backend contracts.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] cargo test --manifest-path src-tauri/Cargo.toml patches::storage
- [ ] cargo test --manifest-path src-tauri/Cargo.toml patches::sources
- [ ] npm run test -- --run src/features/library/state src/features/library/api
- [ ] npm run build
</verification>

<success_criteria>

- All tasks completed
- Valid local `.patch.toml` files can be imported and invalid ones are rejected before persistence
- Supported remote community patch files can be fetched for detected titles with provenance and last-fetched metadata tracked
- Imported and fetched patch files can coexist per game, with one explicit active file selection persisted independently from the patch source files

</success_criteria>

<output>
After completion, create `.planning/phases/05-patch-management/05-01-SUMMARY.md`
</output>
