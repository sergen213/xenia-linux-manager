---
phase: 05-patch-management
plan: 02
type: execute
wave: 2
depends_on: [01]
files_modified:
  - src-tauri/src/patches/storage.rs
  - src-tauri/src/patches/editor.rs
  - src-tauri/src/commands/patches.rs
  - src-tauri/src/lib.rs
  - src/features/library/api/libraryClient.ts
  - src/features/library/model/patchTypes.ts
  - src/features/library/state/libraryStore.ts
  - src/features/library/components/ManagePatchesPanel.tsx
  - src/features/library/components/ActivePatchChooserDialog.tsx
  - src/features/library/components/PatchEntryChecklist.tsx
  - src/features/library/components/PatchImportDropzone.tsx
  - src/features/library/components/GameDetailsPanel.tsx
  - src/features/library/LibraryPage.tsx
autonomous: true
requirements: [PATC-01, PATC-02, PATC-03]
user_setup: []
must_haves:
    truths:
      - The game detail workflow exposes a `Manage patches` affordance that opens patch management on demand instead of cluttering the default detail view
      - Users can choose which installed patch file is active, are prompted to make that choice immediately after a successful import or fetch, and can leave the game intentionally unpatched if no active file is selected
      - Patch entries render as a flat checklist with per-file enable or disable persistence, conflict or compatibility issues show inline warnings only, and unsupported remote states use the exact `No community patch available` messaging
    artifacts:
      - path: src/features/library/components/ManagePatchesPanel.tsx
        provides: Detail-scoped patch management surface with source actions, patch-file summaries, provenance metadata, and entry-point controls for chooser and editor workflows
        min_lines: 110
      - path: src/features/library/components/PatchEntryChecklist.tsx
        provides: Flat checklist editor for patch entries with toggle actions and inline warnings for conflicts or incomplete compatibility metadata
        min_lines: 100
      - path: src/features/library/components/ActivePatchChooserDialog.tsx
        provides: Prompt and selection flow for choosing the active patch file after import, fetch, or manual switching between installed files
        min_lines: 80
      - path: src/features/library/state/libraryStore.ts
        provides: Renderer state and actions for manage-patches visibility, post-import chooser prompts, per-entry toggle persistence, and unsupported-state messaging
        min_lines: 140
    key_links:
      - from: src/features/library/components/GameDetailsPanel.tsx
        to: src/features/library/components/ManagePatchesPanel.tsx
        via: Patch controls stay inside the detail-first library workflow behind an explicit manage affordance
        pattern: manage|patch|detail
      - from: src/features/library/components/ManagePatchesPanel.tsx
        to: src/features/library/components/ActivePatchChooserDialog.tsx
        via: Successful import or fetch immediately routes the user into active patch selection rather than leaving multiple installed files ambiguous
        pattern: active|choose|prompt
      - from: src/features/library/components/PatchEntryChecklist.tsx
        to: src-tauri/src/commands/patches.rs
        via: Entry toggles and warning refreshes persist through backend commands instead of mutating renderer-only copies of patch files
        pattern: toggle|warning|entry|patch
      - from: src/features/library/state/libraryStore.ts
        to: src/features/library/components/PatchImportDropzone.tsx
        via: Manual file picking and drag-and-drop import share the same operation state and completion flow
        pattern: import|drop|pick|patch
---

<objective>
Build patch editor UI with per-entry enable or disable persistence.

Purpose: Turn the backend patch inventory into a usable per-game management workflow where users can import or fetch patch files, choose the active one, and toggle individual entries from the library detail experience without blocking on later profile work.
Output: Detail-view patch management surfaces, active-file chooser flow, drag-and-drop and file-picker import UI, entry checklist editor, and renderer state for toggle persistence and warnings.
</objective>

<execution_context>
@/home/sergeng/.codex/get-shit-done/workflows/execute-plan.md
@/home/sergeng/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-patch-management/05-CONTEXT.md
@.planning/phases/05-patch-management/05-RESEARCH.md
@.planning/phases/05-patch-management/05-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend patch commands and library state for chooser prompts, toggles, and warnings</name>
  <files>src-tauri/src/patches/storage.rs, src-tauri/src/patches/editor.rs, src-tauri/src/commands/patches.rs, src-tauri/src/lib.rs, src/features/library/api/libraryClient.ts, src/features/library/model/patchTypes.ts, src/features/library/state/libraryStore.ts</files>
  <action>Expose editor-ready patch file and entry payloads from the backend, including per-entry enabled state, likely-conflict or incomplete-entry warnings, and the exact unsupported remote state required by context. Extend the library reducer state so patch-management visibility, post-import active-file prompts, and per-entry toggle operations remain consistent across file switching, imports, and fetches.</action>
  <verify><automated>cargo test --manifest-path src-tauri/Cargo.toml patches::editor</automated></verify>
  <done>The renderer can load editor-ready patch data, persist entry toggles per installed patch file, and react cleanly to chooser-prompt and unsupported-remote states.</done>
</task>

<task type="auto">
  <name>Task 2: Build the patch chooser, import surface, and checklist editor inside the library detail domain</name>
  <files>src/features/library/components/ManagePatchesPanel.tsx, src/features/library/components/ActivePatchChooserDialog.tsx, src/features/library/components/PatchEntryChecklist.tsx, src/features/library/components/PatchImportDropzone.tsx</files>
  <action>Build the user-facing patch management surfaces behind a `Manage patches` affordance. Support both drag-and-drop and manual file picking for local imports, present installed patch files with provenance and last-updated metadata, prompt the user to choose the active file immediately after a successful import or fetch, and render patch entries as a flat checklist with inline warnings that do not block editing.</action>
  <verify><automated>npm run test -- --run src/features/library/components</automated></verify>
  <done>Users can open patch management for a game, import or fetch patch files, choose the active one, and edit entry-level enabled state from a flat checklist UI.</done>
</task>

<task type="auto">
  <name>Task 3: Integrate patch management into the game detail flow and preserve empty-state behavior</name>
  <files>src/features/library/components/GameDetailsPanel.tsx, src/features/library/LibraryPage.tsx, src/features/library/state/libraryStore.ts</files>
  <action>Wire the patch-management surfaces into the detail-first library workflow so patch controls stay contextual to the selected game rather than becoming a detached settings screen. Preserve the intentional empty states from context: multiple patch files with no active selection leaves the game unpatched, and unsupported remote fetches show `No community patch available` exactly while still allowing local import actions.</action>
  <verify><automated>npm run test -- --run src/features/library</automated></verify>
  <done>The game detail experience can reveal patch management on demand, preserve the intended empty and unsupported states, and keep patch choices scoped to the selected title.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] cargo test --manifest-path src-tauri/Cargo.toml patches::editor
- [ ] npm run test -- --run src/features/library/components
- [ ] npm run test -- --run src/features/library
- [ ] npm run build
</verification>

<success_criteria>

- All tasks completed
- Patch controls appear behind an explicit `Manage patches` affordance inside the game detail flow
- Users are prompted to choose the active patch file immediately after import or fetch and can intentionally leave a game unpatched if none is selected
- Patch entries render as a flat checklist with per-file toggle persistence and inline warning-only conflict or compatibility messaging

</success_criteria>

<output>
After completion, create `.planning/phases/05-patch-management/05-02-SUMMARY.md`
</output>
