---
phase: 01-app-foundation-and-settings
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - src-tauri/Cargo.toml
  - src/main.tsx
  - src/App.tsx
  - src/app/router.tsx
  - src/components/app-shell/AppShell.tsx
  - src/components/app-shell/Sidebar.tsx
  - src/features/dashboard/DashboardHome.tsx
  - src/features/tasks/TasksPage.tsx
  - src/features/settings/SettingsPage.tsx
  - src/styles/app.css
autonomous: true
requirements: [APP-01]
user_setup: []
must_haves:
    truths:
      - User can launch the desktop app into a Linux-native shell without the renderer crashing
      - User sees a left-sidebar manager layout with Dashboard, Library, Tasks, and Settings sections
      - User sees a compact system status surface in the shell from the first runnable build
    artifacts:
      - path: src/App.tsx
        provides: Root shell composition and section routing
        min_lines: 40
      - path: src/components/app-shell/AppShell.tsx
        provides: Shared sidebar layout and status region
        min_lines: 60
      - path: src/app/router.tsx
        provides: Route and section registration for Phase 1 surfaces
        min_lines: 30
    key_links:
      - from: src/App.tsx
        to: src/components/app-shell/AppShell.tsx
        via: Root app render tree
        pattern: AppShell
      - from: src/components/app-shell/Sidebar.tsx
        to: src/app/router.tsx
        via: Navigation items bound to registered sections
        pattern: Dashboard|Tasks|Settings|Library
      - from: src/components/app-shell/AppShell.tsx
        to: src/features/dashboard/DashboardHome.tsx
        via: Default shell outlet on launch
        pattern: DashboardHome
---

<objective>
Create the initial Tauri/React workspace and the real Linux desktop shell for the manager.

Purpose: Establish the application structure that all later settings, install, library, and task workflows plug into.
Output: Runnable desktop workspace, left-sidebar shell, dashboard landing page, placeholder future sections, and a persistent top-level status area.
</objective>

<execution_context>
@/home/sergeng/.codex/get-shit-done/workflows/execute-plan.md
@/home/sergeng/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-app-foundation-and-settings/01-CONTEXT.md
@.planning/phases/01-app-foundation-and-settings/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold the Tauri + React workspace for Linux development</name>
  <files>package.json, src-tauri/Cargo.toml, src/main.tsx, src/styles/app.css</files>
  <action>Create the Tauri 2 + React + TypeScript workspace with a buildable Linux desktop entrypoint. Add only the baseline dependencies needed for a desktop shell and app styling, and keep background-work or settings-specific libraries out of this plan so later plans own those decisions cleanly.</action>
  <verify><automated>npm run build</automated></verify>
  <done>The repository has a buildable desktop workspace with React mounted through Tauri and a shared stylesheet loaded from the renderer entrypoint.</done>
</task>

<task type="auto">
  <name>Task 2: Build the left-sidebar shell and section routing contracts</name>
  <files>src/App.tsx, src/app/router.tsx, src/components/app-shell/AppShell.tsx, src/components/app-shell/Sidebar.tsx, src/features/dashboard/DashboardHome.tsx, src/features/tasks/TasksPage.tsx, src/features/settings/SettingsPage.tsx</files>
  <action>Implement the real application shell, not a temporary setup screen: a left sidebar, dashboard-first default route, visible future sections including Library, Tasks, and Settings, and placeholder section content where later phases will extend behavior. Make the shell feel like a utility app and keep the dashboard library-centric even in empty state.</action>
  <verify><automated>npm run test -- --run src/components/app-shell src/app/router</automated></verify>
  <done>Launching the renderer shows a dashboard/home experience inside a persistent sidebar shell, and all Phase 1 sections are addressable through the route registry without dead links.</done>
</task>

<task type="auto">
  <name>Task 3: Add the always-visible system status surface</name>
  <files>src/components/app-shell/AppShell.tsx, src/features/dashboard/DashboardHome.tsx</files>
  <action>Add a compact status region to the shell that can later summarize install, scan, and task activity. Keep it lightweight and reusable rather than coupling it to later job infrastructure; it should expose empty and placeholder states that future plans can hydrate with real data.</action>
  <verify><automated>npm run lint</automated></verify>
  <done>The shell includes a visible system status surface from day one, and the dashboard reflects the manager's library-centric product direction even before library data exists.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] npm run build
- [ ] npm run test -- --run src/components/app-shell src/app/router
- [ ] npm run lint
</verification>

<success_criteria>

- All tasks completed
- The Linux desktop shell launches with stable routing and no placeholder crash paths
- Sidebar navigation exposes Dashboard, Library, Tasks, and Settings from the first runnable build
- A compact system status area is present in the shell

</success_criteria>

<output>
After completion, create `.planning/phases/01-app-foundation-and-settings/01-01-SUMMARY.md`
</output>
