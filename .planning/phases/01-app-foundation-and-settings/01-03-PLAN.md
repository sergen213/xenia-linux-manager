---
phase: 01-app-foundation-and-settings
plan: 03
type: execute
wave: 3
depends_on: [01, 02]
files_modified:
  - src-tauri/src/jobs/mod.rs
  - src-tauri/src/jobs/store.rs
  - src-tauri/src/jobs/events.rs
  - src-tauri/src/commands/jobs.rs
  - src/features/tasks/api/tasksClient.ts
  - src/features/tasks/model/taskTypes.ts
  - src/features/tasks/components/TaskStatusStrip.tsx
  - src/features/tasks/components/TaskHistoryCard.tsx
  - src/features/tasks/TasksPage.tsx
  - src/features/tasks/state/tasksStore.ts
  - src/features/dashboard/DashboardHome.tsx
autonomous: true
requirements: [APP-01]
user_setup: []
must_haves:
    truths:
      - Long-running manager jobs report progress and step-by-step logs without freezing the UI
      - Completed and failed jobs remain visible in persistent task history cards
      - If the app closes during a job, the next launch shows that the job was interrupted and allows the user to retry it
    artifacts:
      - path: src-tauri/src/jobs/mod.rs
        provides: Backend job orchestration contract and lifecycle state
        min_lines: 80
      - path: src-tauri/src/jobs/store.rs
        provides: Persisted task history storage and interrupted-job recovery markers
        min_lines: 60
      - path: src/features/tasks/state/tasksStore.ts
        provides: Renderer subscription and task-history state
        min_lines: 50
      - path: src/features/tasks/TasksPage.tsx
        provides: Dedicated Tasks screen with logs and history cards
        min_lines: 50
    key_links:
      - from: src-tauri/src/jobs/events.rs
        to: src/features/tasks/api/tasksClient.ts
        via: Tauri event stream consumed by renderer task subscriptions
        pattern: listen|event
      - from: src/features/tasks/state/tasksStore.ts
        to: src/features/tasks/components/TaskHistoryCard.tsx
        via: Persisted task state rendered as cards
        pattern: TaskHistoryCard
      - from: src/features/dashboard/DashboardHome.tsx
        to: src/features/tasks/components/TaskStatusStrip.tsx
        via: Compact shell/dashboard task summary surface
        pattern: TaskStatusStrip
---

<objective>
Add the backend job/progress infrastructure and baseline logging surfaces for Phase 1.

Purpose: Deliver the non-freezing progress visibility later install, scan, patch, and save workflows will all depend on.
Output: Backend job lifecycle/store modules, renderer task subscription state, dedicated Tasks page, compact task status strip, persisted task history, and interrupted-job recovery messaging.
</objective>

<execution_context>
@/home/sergeng/.codex/get-shit-done/workflows/execute-plan.md
@/home/sergeng/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-app-foundation-and-settings/01-CONTEXT.md
@.planning/phases/01-app-foundation-and-settings/01-RESEARCH.md
@.planning/phases/01-app-foundation-and-settings/01-01-SUMMARY.md
@.planning/phases/01-app-foundation-and-settings/01-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement backend job lifecycle, event streaming, and persistence</name>
  <files>src-tauri/src/jobs/mod.rs, src-tauri/src/jobs/store.rs, src-tauri/src/jobs/events.rs, src-tauri/src/commands/jobs.rs</files>
  <action>Create the Rust-side job subsystem that can register jobs, append step logs, publish progress updates to the renderer, persist task history under the app data path, and mark in-flight jobs as interrupted on unclean shutdown. Design it as shared infrastructure for later install, scan, patch, and save operations rather than a one-off Phase 1 demo implementation.</action>
  <verify><automated>cargo test --manifest-path src-tauri/Cargo.toml jobs</automated></verify>
  <done>The backend exposes a reusable job API with persisted history, progress updates, and explicit interrupted-job state that survives restart.</done>
</task>

<task type="auto">
  <name>Task 2: Build renderer task state, dedicated Tasks page, and shell summary strip</name>
  <files>src/features/tasks/api/tasksClient.ts, src/features/tasks/model/taskTypes.ts, src/features/tasks/components/TaskStatusStrip.tsx, src/features/tasks/components/TaskHistoryCard.tsx, src/features/tasks/TasksPage.tsx, src/features/tasks/state/tasksStore.ts, src/features/dashboard/DashboardHome.tsx</files>
  <action>Implement the renderer-side task store and UI surfaces that consume backend job events and persisted history. Show step-by-step logs in the Tasks page, keep completed and failed jobs as persistent cards, expose a Clear action for history, and add a compact summary strip visible from the main shell/dashboard while background work continues.</action>
  <verify><automated>npm run test -- --run src/features/tasks</automated></verify>
  <done>Users can inspect active, completed, failed, and interrupted jobs from both the dedicated Tasks section and the compact shell summary surface without blocking other app navigation.</done>
</task>

<task type="auto">
  <name>Task 3: Restore interrupted jobs and offer retry-safe restart messaging</name>
  <files>src/features/tasks/state/tasksStore.ts, src/features/tasks/TasksPage.tsx</files>
  <action>Extend startup behavior so persisted in-flight jobs are surfaced as interrupted after restart and the UI offers retry-safe messaging instead of pretending the task succeeded. Keep the retry action generic so later install and scan plans can hook real job handlers into the same recovery path.</action>
  <verify><automated>npm run build</automated></verify>
  <done>After an interrupted run, the next launch surfaces the unfinished job as interrupted, shows recovery guidance, and preserves the rest of the task history.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] cargo test --manifest-path src-tauri/Cargo.toml jobs
- [ ] npm run test -- --run src/features/tasks
- [ ] npm run build
</verification>

<success_criteria>

- All tasks completed
- Background jobs can report progress and logs without freezing the UI
- The Tasks section and shell summary both expose persistent task visibility
- Interrupted jobs are surfaced on restart with retry-safe messaging

</success_criteria>

<output>
After completion, create `.planning/phases/01-app-foundation-and-settings/01-03-SUMMARY.md`
</output>
