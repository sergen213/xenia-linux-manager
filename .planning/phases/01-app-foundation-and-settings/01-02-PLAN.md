---
phase: 01-app-foundation-and-settings
plan: 02
type: execute
wave: 2
depends_on: [01]
files_modified:
  - src-tauri/src/settings/mod.rs
  - src-tauri/src/settings/path_defaults.rs
  - src-tauri/src/settings/path_validation.rs
  - src-tauri/src/commands/settings.rs
  - src/features/settings/api/settingsClient.ts
  - src/features/settings/model/settingsSchema.ts
  - src/features/settings/components/FirstRunSetup.tsx
  - src/features/settings/components/EditPathsDialog.tsx
  - src/features/settings/SettingsPage.tsx
  - src/features/settings/state/settingsStore.ts
autonomous: true
requirements: [XEN-01]
user_setup: []
must_haves:
    truths:
      - First launch is gated until the user confirms storage paths for Xenia, app data, and library metadata
      - The app proposes recommended default paths and restores saved choices on restart
      - Invalid or unwritable path selections are rejected with a warning and the app can recover to defaults
    artifacts:
      - path: src-tauri/src/settings/mod.rs
        provides: Persisted settings service for core storage paths
        min_lines: 80
      - path: src-tauri/src/settings/path_validation.rs
        provides: Filesystem validation and fallback logic for managed paths
        min_lines: 50
      - path: src/features/settings/components/FirstRunSetup.tsx
        provides: Gated first-run path confirmation flow
        min_lines: 60
      - path: src/features/settings/state/settingsStore.ts
        provides: Renderer state for loading, saving, and restoring settings
        min_lines: 40
    key_links:
      - from: src/features/settings/components/FirstRunSetup.tsx
        to: src/features/settings/api/settingsClient.ts
        via: Confirm-path submission workflow
        pattern: save|confirm|initialize
      - from: src/features/settings/api/settingsClient.ts
        to: src-tauri/src/commands/settings.rs
        via: Tauri invoke bridge for settings commands
        pattern: invoke
      - from: src-tauri/src/commands/settings.rs
        to: src-tauri/src/settings/path_validation.rs
        via: Backend validation before persistence
        pattern: validate
---

<objective>
Implement persisted settings and the filesystem path model for Phase 1.

Purpose: Satisfy the manager's first real user requirement by letting users confirm and later edit where Xenia, app data, and library metadata live.
Output: Backend settings service, path validation/default logic, first-run setup flow, edit-path workflow, and renderer state that restores settings on restart.
</objective>

<execution_context>
@/home/sergeng/.codex/get-shit-done/workflows/execute-plan.md
@/home/sergeng/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-app-foundation-and-settings/01-CONTEXT.md
@.planning/phases/01-app-foundation-and-settings/01-RESEARCH.md
@.planning/phases/01-app-foundation-and-settings/01-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement the backend settings service and managed path validation</name>
  <files>src-tauri/src/settings/mod.rs, src-tauri/src/settings/path_defaults.rs, src-tauri/src/settings/path_validation.rs, src-tauri/src/commands/settings.rs</files>
  <action>Create the Rust-side settings subsystem that resolves recommended defaults, validates candidate paths, persists the approved settings document, and exposes load/save/edit commands to the renderer. Keep path logic centralized here so later install, scan, patch, and save features consume one authoritative settings service.</action>
  <verify><automated>cargo test --manifest-path src-tauri/Cargo.toml settings</automated></verify>
  <done>The backend can load persisted settings, compute defaults for first launch, reject invalid or unwritable paths, and return a fallback-safe result to the renderer.</done>
</task>

<task type="auto">
  <name>Task 2: Build the first-run setup flow and later path-edit experience</name>
  <files>src/features/settings/api/settingsClient.ts, src/features/settings/model/settingsSchema.ts, src/features/settings/components/FirstRunSetup.tsx, src/features/settings/components/EditPathsDialog.tsx, src/features/settings/SettingsPage.tsx, src/features/settings/state/settingsStore.ts</files>
  <action>Implement the gated onboarding path confirmation flow plus an Edit paths workflow in Settings. The UI must propose defaults, allow edits before confirmation, explain path changes with an impact summary, and keep the user blocked from the main app until the three core paths are in a valid persisted state.</action>
  <verify><automated>npm run test -- --run src/features/settings</automated></verify>
  <done>First launch stays inside setup until paths are confirmed, later edits are available from Settings, and the renderer shows clear warnings when a chosen path is invalid or unavailable.</done>
</task>

<task type="auto">
  <name>Task 3: Restore settings and last-known navigation state on restart</name>
  <files>src/features/settings/state/settingsStore.ts, src/features/settings/SettingsPage.tsx</files>
  <action>Extend the renderer settings store to restore persisted path choices and last-open section/sidebar state during startup. Keep this restart behavior explicit so later task-history recovery can build on the same app bootstrap flow rather than adding a second startup pathway.</action>
  <verify><automated>npm run build</automated></verify>
  <done>Restarting the app restores the confirmed paths and returns the user to the expected shell state instead of forcing setup again unless persisted settings are invalid.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] cargo test --manifest-path src-tauri/Cargo.toml settings
- [ ] npm run test -- --run src/features/settings
- [ ] npm run build
</verification>

<success_criteria>

- All tasks completed
- Users can confirm or edit recommended storage paths on first launch
- Saved path choices restore on restart
- Invalid path selections are handled safely with warnings and fallback behavior

</success_criteria>

<output>
After completion, create `.planning/phases/01-app-foundation-and-settings/01-02-SUMMARY.md`
</output>
