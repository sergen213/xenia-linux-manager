---
phase: 03-library-source-management-and-scanning
plan: 03
type: execute
wave: 3
depends_on: [01, 02]
files_modified:
  - src-tauri/src/library/scan_jobs.rs
autonomous: true
requirements: [LIB-01, LIB-02, LIB-03]
user_setup: []
must_haves:
    truths:
      - Queued and startup-triggered scans automatically advance to the next request after an active scan completes, fails, or is cancelled
      - Scan jobs still persist partial or complete discovery results and emit terminal job events without leaving the scan coordinator stuck in an active state
      - The scan coordinator documentation and regression coverage reflect the real discovery pipeline instead of stale placeholder behavior
    artifacts:
      - path: src-tauri/src/library/scan_jobs.rs
        provides: Runtime coordinator completion handoff plus regression coverage for queued follow-up scans and terminal cleanup
        min_lines: 180
    key_links:
      - from: src-tauri/src/library/scan_jobs.rs
        to: src-tauri/src/library/scan_jobs.rs
        via: Every terminal runtime path notifies `ScanCoordinator::finish_scan` so queued work can start immediately after the current scan ends
        pattern: finish_scan
      - from: src-tauri/src/library/scan_jobs.rs
        to: src-tauri/src/library/catalog.rs
        via: Discovery results remain persisted before the coordinator releases the active slot and advances queued work
        pattern: persist|complete|fail|cancel
---

<objective>
Restore real queue progression for library scans by wiring runtime scan completion back into the coordinator.

Purpose: Close the verified phase-3 blocker where queued scans stall after the first active job because the coordinator never releases the active slot.
Output: Backend scan runtime cleanup that always calls `finish_scan`, regression tests for queued follow-up execution, and refreshed coordinator comments that match the current discovery/catalog implementation.
</objective>

<execution_context>
@/home/sergeng/.codex/get-shit-done/workflows/execute-plan.md
@/home/sergeng/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-library-source-management-and-scanning/03-CONTEXT.md
@.planning/phases/03-library-source-management-and-scanning/03-RESEARCH.md
@.planning/phases/03-library-source-management-and-scanning/03-VERIFICATION.md
@.planning/phases/03-library-source-management-and-scanning/03-01-SUMMARY.md
@.planning/phases/03-library-source-management-and-scanning/03-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire runtime scan termination back into the coordinator</name>
  <files>src-tauri/src/library/scan_jobs.rs</files>
  <action>Refactor the async scan execution path so `ScanCoordinator::finish_scan` is invoked exactly once whenever `run_scan_job` exits, regardless of success, cancellation, missing source, inaccessible path, or catalog persistence failure. Use a cleanup pattern that preserves the existing job completion/failure events and result persistence semantics while guaranteeing the active slot is released before queued follow-up work is considered.</action>
  <verify><automated>cargo test --manifest-path src-tauri/Cargo.toml library::scan_jobs</automated></verify>
  <done>Queued scans no longer stall because every terminal runtime path releases the active job and allows the coordinator to start the next queued request.</done>
</task>

<task type="auto">
  <name>Task 2: Add regression coverage for queue progression and terminal cleanup</name>
  <files>src-tauri/src/library/scan_jobs.rs</files>
  <action>Add backend tests that prove queued multi-source scans progress after the first scan finishes and that cancellation or failure paths clear active/cancelled coordinator state instead of leaving the queue blocked. Cover the specific bug from verification by asserting queued work can advance after a terminal path that previously returned early.</action>
  <verify><automated>cargo test --manifest-path src-tauri/Cargo.toml library::scan_jobs</automated></verify>
  <done>The blocker has a durable regression test suite, so future scan-runtime changes cannot silently reintroduce a stuck queue.</done>
</task>

<task type="auto">
  <name>Task 3: Remove stale placeholder guidance and rerun focused phase-3 verification</name>
  <files>src-tauri/src/library/scan_jobs.rs</files>
  <action>Replace outdated comments that still describe placeholder-only scan behavior with accurate notes about the current discovery/catalog flow and the coordinator handoff contract. Re-run the focused backend verification commands needed to confirm queue progression still coexists with discovery persistence and partial-result handling.</action>
  <verify><automated>cargo test --manifest-path src-tauri/Cargo.toml library::discovery && cargo test --manifest-path src-tauri/Cargo.toml library::catalog</automated></verify>
  <done>The implementation guidance matches reality, and the phase-3 backend checks confirm the queue fix did not regress discovery or catalog persistence.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] cargo test --manifest-path src-tauri/Cargo.toml library::scan_jobs
- [ ] cargo test --manifest-path src-tauri/Cargo.toml library::discovery
- [ ] cargo test --manifest-path src-tauri/Cargo.toml library::catalog
</verification>

<success_criteria>

- All tasks completed
- Queued scans and startup-triggered rescans advance automatically after each scan finishes, fails, or is cancelled
- Discovery persistence and terminal job events remain intact while the coordinator releases the active slot correctly
- `scan_jobs.rs` comments and regression tests describe and enforce the real runtime contract

</success_criteria>

<output>
After completion, create `.planning/phases/03-library-source-management-and-scanning/03-03-SUMMARY.md`
</output>
