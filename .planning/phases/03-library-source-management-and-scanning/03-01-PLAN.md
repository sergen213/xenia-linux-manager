---
phase: 03-library-source-management-and-scanning
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src-tauri/src/library/mod.rs
  - src-tauri/src/library/sources.rs
  - src-tauri/src/library/scan_jobs.rs
  - src-tauri/src/commands/library.rs
  - src-tauri/src/commands/mod.rs
  - src-tauri/src/lib.rs
  - src/features/library/api/libraryClient.ts
  - src/features/library/model/libraryTypes.ts
  - src/features/library/state/libraryStore.ts
  - src/features/library/state/LibraryProvider.tsx
  - src/features/library/components/LibrarySourcesPanel.tsx
  - src/features/library/LibraryPage.tsx
  - src/App.tsx
autonomous: true
requirements: [LIB-01, LIB-02, LIB-03]
user_setup: []
must_haves:
    truths:
      - Users can add, list, rescan, and remove one or more library source folders from the app, with nested-source warnings surfaced instead of silent rejection
      - Source registration and scan orchestration are backend-owned and persisted under the configured library metadata path rather than living only in React state
      - Scan jobs reuse the shared task/job system and support queueing, cancellation, per-source scans, startup rescan hooks, and a `Scan All Now` override that launches all sources concurrently
    artifacts:
      - path: src-tauri/src/library/sources.rs
        provides: Persisted source model plus add/remove/list validation and nested-path warning logic
        min_lines: 90
      - path: src-tauri/src/library/scan_jobs.rs
        provides: Background scan coordinator with queue, cancel, and concurrent override behavior
        min_lines: 110
      - path: src/features/library/state/libraryStore.ts
        provides: Renderer library-source state and scan action wiring using the existing provider/reducer pattern
        min_lines: 80
      - path: src/features/library/components/LibrarySourcesPanel.tsx
        provides: User-facing source management and scan controls on the Library page
        min_lines: 90
    key_links:
      - from: src/features/library/api/libraryClient.ts
        to: src-tauri/src/commands/library.rs
        via: Renderer invokes typed backend commands for source registration, source removal, scan start, scan cancel, and library status loading
        pattern: invoke
      - from: src-tauri/src/commands/library.rs
        to: src-tauri/src/library/sources.rs
        via: Library commands expose persisted source configuration and nested-source warnings to the renderer
        pattern: source|warning|list|remove
      - from: src-tauri/src/commands/library.rs
        to: src-tauri/src/library/scan_jobs.rs
        via: Scan commands map UI actions to queued, cancelled, or concurrent background scan execution
        pattern: scan|cancel|all
      - from: src/features/library/state/LibraryProvider.tsx
        to: src/features/tasks/state/TasksProvider.tsx
        via: Library scan progress stays on the shared job event pipeline instead of introducing a parallel progress mechanism
        pattern: job|scan
---

<objective>
Implement folder source management and scan job orchestration for the library workflow.

Purpose: Give the app a persisted source registry and a real scan-control contract before adding content discovery logic.
Output: Backend source persistence, library command surface, scan coordinator, renderer library provider, and Library page controls for add/remove/rescan/cancel/scan-all actions.
</objective>

<execution_context>
@/home/sergeng/.codex/get-shit-done/workflows/execute-plan.md
@/home/sergeng/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-library-source-management-and-scanning/03-CONTEXT.md
@.planning/phases/03-library-source-management-and-scanning/03-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create persisted library-source storage and validation on the backend</name>
  <files>src-tauri/src/library/sources.rs, src-tauri/src/library/mod.rs, src-tauri/src/commands/library.rs, src-tauri/src/lib.rs</files>
  <action>Create the backend source document stored under `library_metadata_path`, with typed records for source id, root path, timestamps, and last scan summary metadata. Implement add/list/remove operations, normalize absolute paths, and emit nested-source warnings when a new path overlaps an existing source. Removing a source must also remove its associated persisted scan data contract, even if Plan 02 fills in the catalog implementation later.</action>
  <verify><automated>cargo test --manifest-path src-tauri/Cargo.toml library::sources</automated></verify>
  <done>The backend can persist and reload library sources, detect nested-path conflicts, and expose a stable source registry to the renderer.</done>
</task>

<task type="auto">
  <name>Task 2: Implement scan job coordination, queueing, cancel, and `Scan All Now` override behavior</name>
  <files>src-tauri/src/library/scan_jobs.rs, src-tauri/src/commands/library.rs, src-tauri/src/lib.rs</files>
  <action>Build the scan coordinator on top of the shared job system so source scans register as `scan` jobs, emit detailed logs/progress, and can be started for one source or all sources. Normal requests should queue while a scan is active; `Scan All Now` must drain queued work and launch all configured source scans concurrently. Add explicit cancellation handling and startup hooks so existing sources can be rescanned when the app initializes.</action>
  <verify><automated>cargo test --manifest-path src-tauri/Cargo.toml library::scan_jobs</automated></verify>
  <done>The app has a backend-owned scan lifecycle with queueing, cancel, and concurrent-all override semantics that match the phase context.</done>
</task>

<task type="auto">
  <name>Task 3: Add the renderer library provider and source-management controls</name>
  <files>src/features/library/api/libraryClient.ts, src/features/library/model/libraryTypes.ts, src/features/library/state/libraryStore.ts, src/features/library/state/LibraryProvider.tsx, src/features/library/components/LibrarySourcesPanel.tsx, src/features/library/LibraryPage.tsx, src/App.tsx</files>
  <action>Create a dedicated library feature client/store/provider following the same context + reducer pattern already used by settings and tasks. Load persisted sources on startup, trigger automatic rescans for existing sources, and expose UI actions for add source, remove source, rescan one source, cancel active scan, and `Scan All Now`. Surface nested-source warnings and current scan status directly in the Library page rather than burying them in generic task history.</action>
  <verify><automated>npm run test -- --run src/features/library/state src/features/library/components</automated></verify>
  <done>Users can manage library source folders and launch scan actions from the Library page using renderer state that stays aligned with backend truth.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] cargo test --manifest-path src-tauri/Cargo.toml library::sources
- [ ] cargo test --manifest-path src-tauri/Cargo.toml library::scan_jobs
- [ ] npm run test -- --run src/features/library/state src/features/library/components
- [ ] npm run test -- --run src/features/tasks
</verification>

<success_criteria>

- All tasks completed
- Users can add and remove one or more library source folders with nested-source warnings surfaced clearly
- Library scans run through the shared background task pipeline with queueing, cancellation, and concurrent `Scan All Now` behavior
- The Library page exposes source management and scan controls without inventing renderer-only source truth

</success_criteria>

<output>
After completion, create `.planning/phases/03-library-source-management-and-scanning/03-01-SUMMARY.md`
</output>
