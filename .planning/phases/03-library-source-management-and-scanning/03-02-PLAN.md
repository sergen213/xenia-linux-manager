---
phase: 03-library-source-management-and-scanning
plan: 02
type: execute
wave: 2
depends_on: [01]
files_modified:
  - src-tauri/src/library/discovery.rs
  - src-tauri/src/library/catalog.rs
  - src-tauri/src/library/mod.rs
  - src-tauri/src/commands/library.rs
  - src/features/library/model/libraryTypes.ts
  - src/features/library/state/libraryStore.ts
  - src/features/library/components/ScanResultsSummary.tsx
  - src/features/library/components/DiscoveryResultsTable.tsx
  - src/features/library/LibraryPage.tsx
  - src/features/dashboard/DashboardHome.tsx
autonomous: true
requirements: [LIB-02, LIB-03]
user_setup: []
must_haves:
    truths:
      - The backend recursively discovers `.xex` entries inside configured source folders and persists them as library candidates tied to a stable source id
      - The backend detects supported ISO-based inputs heuristically, marks uncertainty explicitly, and persists low-confidence candidates instead of discarding them silently
      - Completed, partial-success, duplicate, warning, and cancelled scan outcomes remain visible to the user through persisted summaries and candidate records that survive app restarts
    artifacts:
      - path: src-tauri/src/library/discovery.rs
        provides: Recursive `.xex` and heuristic ISO candidate detection with duplicate/warning annotations
        min_lines: 120
      - path: src-tauri/src/library/catalog.rs
        provides: Persisted scan-results catalog and source-level summary model for later library and review flows
        min_lines: 110
      - path: src/features/library/components/DiscoveryResultsTable.tsx
        provides: Library UI surface for found candidates, duplicates, warnings, and low-confidence ISO detections
        min_lines: 90
      - path: src/features/library/components/ScanResultsSummary.tsx
        provides: User-facing summary of found, skipped, duplicate, warning, and partial/cancelled outcomes
        min_lines: 70
    key_links:
      - from: src-tauri/src/library/scan_jobs.rs
        to: src-tauri/src/library/discovery.rs
        via: Running scan jobs call the discovery engine and persist incremental candidate results as traversal progresses
        pattern: discover|scan|candidate
      - from: src-tauri/src/library/discovery.rs
        to: src-tauri/src/library/catalog.rs
        via: Detected `.xex` and ISO candidates are normalized and written into the persisted scan catalog with status and confidence metadata
        pattern: persist|result|summary
      - from: src/features/library/state/libraryStore.ts
        to: src/features/library/components/ScanResultsSummary.tsx
        via: Renderer library state exposes persisted scan summaries and candidate lists directly to the Library page
        pattern: summary|warning|duplicate
      - from: src/features/library/components/DiscoveryResultsTable.tsx
        to: src/features/dashboard/DashboardHome.tsx
        via: Scan-result counts feed top-level library visibility so the dashboard can report detected entries and recent scan outcomes
        pattern: count|detected|recent
---

<objective>
Build `.xex` and ISO candidate discovery with persisted scan results.

Purpose: Turn the source-management and scan orchestration layer into a real library-ingestion pipeline that preserves useful evidence for later review and launch work.
Output: Backend discovery engine, persisted scan catalog, duplicate/warning annotations, and Library UI summary/results surfaces for completed and partial scans.
</objective>

<execution_context>
@/home/sergeng/.codex/get-shit-done/workflows/execute-plan.md
@/home/sergeng/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-library-source-management-and-scanning/03-CONTEXT.md
@.planning/phases/03-library-source-management-and-scanning/03-RESEARCH.md
@.planning/phases/03-library-source-management-and-scanning/03-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement recursive `.xex` discovery and heuristic ISO candidate detection</name>
  <files>src-tauri/src/library/discovery.rs, src-tauri/src/library/mod.rs, src-tauri/src/commands/library.rs</files>
  <action>Create the filesystem discovery engine that walks registered source folders recursively, identifies `.xex` files deterministically, and detects ISO-backed inputs heuristically from supported file patterns. Normalize candidate metadata, derive display labels from filenames or paths, and annotate duplicates, unreadable paths, suspicious entries, and low-confidence ISO findings instead of silently dropping them.</action>
  <verify><automated>cargo test --manifest-path src-tauri/Cargo.toml library::discovery</automated></verify>
  <done>The backend can scan a source tree and return structured `.xex` plus ISO candidate records with status, warning, and confidence metadata.</done>
</task>

<task type="auto">
  <name>Task 2: Persist scan results and preserve partial discoveries across warnings and cancellation</name>
  <files>src-tauri/src/library/catalog.rs, src-tauri/src/library/discovery.rs, src-tauri/src/commands/library.rs</files>
  <action>Store discovered candidates and per-source scan summaries under `library_metadata_path` in a catalog separate from the source registry. Update the scan pipeline so partial findings are written incrementally, cancellation keeps already discovered entries, and permission or mount errors yield partial-success summaries instead of hard failure whenever possible. Persist duplicate counts, skipped items, warnings, and final scan outcome for later review flows.</action>
  <verify><automated>cargo test --manifest-path src-tauri/Cargo.toml library::catalog</automated></verify>
  <done>Scan results survive app restarts and preserve useful discoveries even when a scan is cancelled or partially blocked by filesystem issues.</done>
</task>

<task type="auto">
  <name>Task 3: Expose scan summaries and discovery results in the Library UI</name>
  <files>src/features/library/model/libraryTypes.ts, src/features/library/state/libraryStore.ts, src/features/library/components/ScanResultsSummary.tsx, src/features/library/components/DiscoveryResultsTable.tsx, src/features/library/LibraryPage.tsx, src/features/dashboard/DashboardHome.tsx</files>
  <action>Extend the library feature state so it loads persisted scan summaries and candidate lists from the backend, then render them in the Library page with explicit sections for found entries, duplicates, warnings, skipped items, and low-confidence ISO candidates. Keep the UI honest about uncertainty and partial-success outcomes, and surface aggregate counts in the dashboard so the app starts feeling like a real library manager rather than a placeholder shell.</action>
  <verify><automated>npm run test -- --run src/features/library src/features/dashboard</automated></verify>
  <done>The Library page shows persisted scan outcomes and candidate details clearly enough for users to understand what was found, what was ambiguous, and what needs later review.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] cargo test --manifest-path src-tauri/Cargo.toml library::discovery
- [ ] cargo test --manifest-path src-tauri/Cargo.toml library::catalog
- [ ] npm run test -- --run src/features/library src/features/dashboard
- [ ] npm run build
</verification>

<success_criteria>

- All tasks completed
- Recursive `.xex` discovery works across configured source folders
- ISO-backed candidates are detected heuristically and persisted with explicit confidence and warning metadata
- Scan summaries and candidate results remain visible after cancellation, partial-success conditions, and app restart

</success_criteria>

<output>
After completion, create `.planning/phases/03-library-source-management-and-scanning/03-02-SUMMARY.md`
</output>
