---
phase: 07-save-portability-and-safety
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src-tauri/Cargo.toml
  - src-tauri/src/saves/mod.rs
  - src-tauri/src/saves/paths.rs
  - src-tauri/src/saves/archive.rs
  - src-tauri/src/saves/import.rs
  - src-tauri/src/saves/storage.rs
  - src-tauri/src/commands/saves.rs
  - src-tauri/src/lib.rs
  - src/features/library/api/libraryClient.ts
  - src/features/library/model/saveTypes.ts
  - src/features/library/state/libraryStore.ts
autonomous: true
requirements: [SAVE-01, SAVE-02, SAVE-03]
user_setup: []
must_haves:
    truths:
      - Save export produces a human-readable `.zip` archive with an explicit manifest plus selected save data, settings, and patch payloads, so the archive is portable and inspectable without app-specific tooling
      - Save import never writes destructive changes directly from the uploaded archive; it validates and stages archive contents, computes a conflict plan, and creates a backup of current local state before any overwrite-capable apply step
      - Conflict handling is backend-owned and deterministic, with explicit support for `Replace all`, `Keep both if possible`, and `Cancel`, so the renderer only presents validated choices instead of guessing filesystem-safe behavior
    artifacts:
      - path: src-tauri/src/saves/paths.rs
        provides: Canonical per-game resolution of save, settings, and patch roots using library identity and app-managed storage boundaries
        min_lines: 120
      - path: src-tauri/src/saves/archive.rs
        provides: Export manifest generation, archive packaging, import manifest validation, and typed portable-archive metadata
        min_lines: 150
      - path: src-tauri/src/saves/import.rs
        provides: Import staging pipeline, conflict-plan generation, backup-before-apply flow, and per-item apply result reporting
        min_lines: 170
      - path: src-tauri/src/commands/saves.rs
        provides: Typed Tauri commands for export preflight, export execution, import inspection, conflict confirmation, and backup-aware apply orchestration
        min_lines: 100
    key_links:
      - from: src-tauri/src/commands/saves.rs
        to: src-tauri/src/saves/import.rs
        via: Renderer-facing save commands return preflight, conflict, and apply-result payloads from one backend-owned import pipeline instead of duplicating destructive logic in the UI
        pattern: save|import|conflict|backup
      - from: src-tauri/src/saves/archive.rs
        to: src-tauri/src/saves/paths.rs
        via: Portable archive creation depends on canonical save, settings, and patch roots for the selected game rather than raw renderer-supplied filesystem paths
        pattern: archive|manifest|path|game
      - from: src/features/library/api/libraryClient.ts
        to: src-tauri/src/commands/saves.rs
        via: The library feature consumes typed save preflight and result contracts for later detail-view and dedicated-saves workflows
        pattern: save|export|import|preview
      - from: src/features/library/state/libraryStore.ts
        to: src/features/library/model/saveTypes.ts
        via: Save workflow summaries, progress state, and selected-game save payloads are modeled explicitly for later reuse across detail and route-level UI
        pattern: save|archive|conflict|result
---

<objective>
Implement save path resolution and archive import/export backend flows.

Purpose: Create the backend safety contract for save portability so the manager can resolve a game's save roots, package portable archives with manifest metadata, inspect imported archives before mutation, and require backup-aware conflict handling for overwrite-capable imports.
Output: Save path resolution, archive manifest and packaging modules, staged import and backup pipeline, typed save commands, and renderer-facing save state contracts.
</objective>

<execution_context>
@/home/sergeng/.codex/get-shit-done/workflows/execute-plan.md
@/home/sergeng/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/07-save-portability-and-safety/7-CONTEXT.md
@.planning/phases/07-save-portability-and-safety/07-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build canonical save path resolution and portable archive packaging</name>
  <files>src-tauri/Cargo.toml, src-tauri/src/saves/mod.rs, src-tauri/src/saves/paths.rs, src-tauri/src/saves/archive.rs, src-tauri/src/saves/storage.rs</files>
  <action>Create a backend-owned saves domain rooted in canonical game identity that can resolve the local save root plus related settings and patch artifacts required for portability. Implement a human-readable zip export format with a typed manifest, archive versioning, human-safe filename generation, and selective export of save slots or folders rather than forcing all-or-nothing export.</action>
  <verify><automated>cargo test --manifest-path src-tauri/Cargo.toml saves::paths saves::archive</automated></verify>
  <done>The backend can resolve per-game save roots, package selected save content with manifest metadata into a normal zip archive, and explain exactly what the export contains.</done>
</task>

<task type="auto">
  <name>Task 2: Implement staged import inspection, conflict planning, and backup-before-apply behavior</name>
  <files>src-tauri/src/saves/import.rs, src-tauri/src/saves/archive.rs, src-tauri/src/saves/storage.rs, src-tauri/src/saves/paths.rs</files>
  <action>Implement an import pipeline that validates the archive manifest, extracts into staging under `app_data_path`, identifies the target game from metadata or explicit selection, compares staged content against local save, settings, and patch state, and generates a conflict plan with explicit actions for `Replace all`, `Keep both if possible`, and `Cancel`. Before any overwrite-capable apply step, create a backup of current local state and preserve per-item apply results so partial success can be explained later.</action>
  <verify><automated>cargo test --manifest-path src-tauri/Cargo.toml saves::import</automated></verify>
  <done>The backend can inspect a save archive safely, produce a side-by-side conflict-ready preview, create a backup before destructive import, and apply the selected policy with detailed per-item results.</done>
</task>

<task type="auto">
  <name>Task 3: Expose typed save commands and library-state wiring for backend save workflows</name>
  <files>src-tauri/src/commands/saves.rs, src-tauri/src/lib.rs, src/features/library/api/libraryClient.ts, src/features/library/model/saveTypes.ts, src/features/library/state/libraryStore.ts</files>
  <action>Expose typed Tauri commands for export preflight, export execution, import archive inspection, conflict confirmation, and backup-aware apply orchestration. Extend the library feature's existing client and reducer state so later game-detail and dedicated-saves surfaces can load save summaries, start save jobs, hold staged import previews, and surface result payloads without reproducing backend filesystem logic.</action>
  <verify><automated>npm run test -- --run src/features/library/state src/features/library/api</automated></verify>
  <done>The renderer can request backend save previews and operations through typed contracts, and the shared library state is ready to support both quick per-game actions and dedicated saves screens.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] cargo test --manifest-path src-tauri/Cargo.toml saves::paths saves::archive
- [ ] cargo test --manifest-path src-tauri/Cargo.toml saves::import
- [ ] npm run test -- --run src/features/library/state src/features/library/api
- [ ] npm run build
</verification>

<success_criteria>

- All tasks completed
- Save export produces a normal zip archive with manifest metadata plus selected save, settings, and patch payloads
- Save import is staged and previewable before mutation, with deterministic conflict-plan output for replace, keep-both, and cancel paths
- Overwrite-capable imports create a backup of current local state before apply and preserve detailed per-item results for later UI reporting

</success_criteria>

<output>
After completion, create `.planning/phases/07-save-portability-and-safety/07-01-SUMMARY.md`
</output>
