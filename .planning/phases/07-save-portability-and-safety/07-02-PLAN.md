---
phase: 07-save-portability-and-safety
plan: 02
type: execute
wave: 2
depends_on: [01]
files_modified:
  - src/app/router.tsx
  - src/features/library/api/libraryClient.ts
  - src/features/library/model/saveTypes.ts
  - src/features/library/state/libraryStore.ts
  - src/features/library/components/GameDetailsPanel.tsx
  - src/features/library/components/SaveQuickActions.tsx
  - src/features/saves/SavesPage.tsx
  - src/features/saves/SavesPage.css
  - src/features/saves/components/SaveExportDialog.tsx
  - src/features/saves/components/SaveImportWizard.tsx
  - src/features/saves/components/SaveConflictPreview.tsx
  - src/features/saves/components/SaveResultsPanel.tsx
  - src/features/saves/components/BackupFailureDialog.tsx
autonomous: true
requirements: [SAVE-01, SAVE-02, SAVE-03]
user_setup: []
must_haves:
    truths:
      - Save import and export actions are available both from the selected game's detail workflow and from a dedicated `Saves` area, with quick actions for common cases and guided flows for advanced or risky operations
      - Any overwrite-capable import shows a side-by-side summary before apply, uses cautious technical warning language, and forces the user through an explicit `Replace all`, `Keep both if possible`, or `Cancel` decision backed by backend preflight data
      - Export success, backup failure, and partial import success all surface concrete artifact locations and per-item outcomes, so the user can trust what happened and recover without reading logs
    artifacts:
      - path: src/features/saves/components/SaveImportWizard.tsx
        provides: Guided archive-first and game-first import flow that consumes staged backend previews, target-game detection, conflict policy choice, and apply confirmation steps
        min_lines: 140
      - path: src/features/saves/components/SaveConflictPreview.tsx
        provides: Side-by-side conflict summary with per-item classifications, policy explanations, and cautious overwrite messaging wired to backend conflict plans
        min_lines: 110
      - path: src/features/saves/components/SaveResultsPanel.tsx
        provides: Detailed export and import result presentation including reveal-folder actions, backup references, and partial-success item reporting
        min_lines: 100
      - path: src/features/library/state/libraryStore.ts
        provides: Shared renderer state for quick-export triggers, import preview lifecycle, backup-risk acceptance, and route-to-detail coordination across library and saves surfaces
        min_lines: 150
    key_links:
      - from: src/features/library/components/GameDetailsPanel.tsx
        to: src/features/library/components/SaveQuickActions.tsx
        via: The game detail workflow offers fast per-title save actions without forcing the user into the full dedicated saves route for common exports and imports
        pattern: save|quick|game|detail
      - from: src/features/saves/SavesPage.tsx
        to: src/features/saves/components/SaveImportWizard.tsx
        via: The dedicated saves route owns the archive-first and guided import flow while reusing the same shared save state as game-level entry points
        pattern: saves|import|wizard|route
      - from: src/features/saves/components/SaveConflictPreview.tsx
        to: src/features/library/model/saveTypes.ts
        via: Conflict previews render backend-generated classifications and allowed policies instead of inferring overwrite safety in the browser
        pattern: conflict|replace|keep both|cancel
      - from: src/features/saves/components/BackupFailureDialog.tsx
        to: src/features/saves/components/SaveResultsPanel.tsx
        via: Backup-risk acceptance and final result reporting stay connected so recovery artifacts remain visible when an import proceeds after backup failure
        pattern: backup|risk|result|recovery
---

<objective>
Add save management UI, overwrite warnings, and recovery messaging.

Purpose: Turn the backend save portability contract into a trustworthy user workflow with quick per-game actions, a dedicated saves route, explicit overwrite previews, and recovery-focused result messaging for exports and imports.
Output: Dedicated saves page, game-detail quick actions, guided import and export flows, side-by-side conflict preview, backup-failure warning dialog, and detailed result surfaces.
</objective>

<execution_context>
@/home/sergeng/.codex/get-shit-done/workflows/execute-plan.md
@/home/sergeng/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/07-save-portability-and-safety/7-CONTEXT.md
@.planning/phases/07-save-portability-and-safety/07-RESEARCH.md
@.planning/phases/07-save-portability-and-safety/07-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend shared save state and route structure for detail-level and dedicated save workflows</name>
  <files>src/app/router.tsx, src/features/library/api/libraryClient.ts, src/features/library/model/saveTypes.ts, src/features/library/state/libraryStore.ts, src/features/saves/SavesPage.tsx, src/features/saves/SavesPage.css</files>
  <action>Add a dedicated `Saves` route to the route registry and extend the shared library save state so the app can coordinate archive-first imports, selected-game quick actions, staged previews, backup-risk acceptance, and final result payloads through one renderer contract. The route structure must support entering the workflow from a selected game or from an archive without fragmenting save state into separate stores.</action>
  <verify><automated>npm run test -- --run src/features/library/state src/features/saves</automated></verify>
  <done>The app has a dedicated saves surface plus shared state that can drive both detail-level actions and full guided save workflows without duplicating backend contracts.</done>
</task>

<task type="auto">
  <name>Task 2: Build the guided export and import interfaces with explicit conflict and backup handling</name>
  <files>src/features/library/components/SaveQuickActions.tsx, src/features/saves/components/SaveExportDialog.tsx, src/features/saves/components/SaveImportWizard.tsx, src/features/saves/components/SaveConflictPreview.tsx, src/features/saves/components/BackupFailureDialog.tsx</files>
  <action>Build quick export and import entry points in the game detail workflow plus guided dialogs and wizards for advanced selection, archive-first import, target-game detection, side-by-side conflict preview, and backup-failure risk acknowledgment. Overwrite-capable imports must use cautious technical messaging and only offer the exact conflict actions approved in context: `Replace all`, `Keep both if possible`, or `Cancel`.</action>
  <verify><automated>npm run test -- --run src/features/saves/components src/features/library/components</automated></verify>
  <done>Users can launch quick save actions from a game, move into advanced guided flows when needed, review overwrites side by side, and explicitly choose how risky imports proceed.</done>
</task>

<task type="auto">
  <name>Task 3: Add export/import success, partial-result, and recovery messaging surfaces</name>
  <files>src/features/saves/components/SaveResultsPanel.tsx, src/features/saves/components/SaveImportWizard.tsx, src/features/saves/SavesPage.tsx, src/features/library/components/GameDetailsPanel.tsx, src/features/library/state/libraryStore.ts</files>
  <action>Implement result surfaces that show where an export archive was written, provide immediate reveal or open-folder actions, report backup artifact locations when created, and explain partial import success at the per-item level instead of collapsing everything into a single summary. Keep the messaging technical and trustworthy, especially when import continues after a backup failure or when some items were skipped or renamed by the selected conflict policy.</action>
  <verify><automated>npm run test -- --run src/features/saves src/features/library</automated></verify>
  <done>The save workflow ends with concrete artifact locations, per-item results, and recovery-ready messaging that matches the safety posture promised by the phase.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] npm run test -- --run src/features/library/state src/features/saves
- [ ] npm run test -- --run src/features/saves/components src/features/library/components
- [ ] npm run test -- --run src/features/saves src/features/library
- [ ] npm run build
</verification>

<success_criteria>

- All tasks completed
- Save actions are available both in the game detail workflow and a dedicated saves area
- Overwrite-capable imports present a side-by-side conflict summary with only the approved conflict policies and cautious technical warnings
- Export and import result screens provide concrete artifact locations, backup references, and per-item reporting for partial success or recovery cases

</success_criteria>

<output>
After completion, create `.planning/phases/07-save-portability-and-safety/07-02-SUMMARY.md`
</output>
