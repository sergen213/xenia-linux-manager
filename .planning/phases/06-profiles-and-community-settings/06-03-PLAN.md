---
phase: 06-profiles-and-community-settings
plan: 03
type: execute
wave: 3
depends_on: [01, 02]
files_modified:
  - src-tauri/src/profiles/materialize.rs
  - src-tauri/src/profiles/merge.rs
  - src-tauri/src/library/launch.rs
  - src-tauri/src/commands/profiles.rs
  - src-tauri/src/commands/library.rs
  - src-tauri/src/lib.rs
  - src/features/library/api/libraryClient.ts
  - src/features/library/model/profileTypes.ts
  - src/features/library/state/libraryStore.ts
  - src/features/library/components/ProfileSummaryCard.tsx
  - src/features/library/components/ProfileEditorPanel.tsx
  - src/features/library/components/ProfileRawEditor.tsx
  - src/features/library/components/UnsavedProfileChangesDialog.tsx
  - src/features/library/components/GameDetailsPanel.tsx
  - src/features/library/components/LaunchPreflightPanel.tsx
autonomous: true
requirements: [PROF-01, PROF-03, PROF-04, LAUN-02]
user_setup: []
must_haves:
    truths:
      - The game detail workflow exposes the active profile name, key changed settings, and a toggle between explicit profile values and the full effective config both before and during editing
      - Users can edit profile settings through labeled fields and a raw editor backed by the same explicit-value document, and navigating away with unsaved changes triggers a warning flow
      - Launch materializes the selected patch and effective profile together into derived config state before spawning Xenia, so launch uses the same effective settings the user reviewed in the UI
    artifacts:
      - path: src-tauri/src/profiles/materialize.rs
        provides: Launch-time materialization of effective profile config into derived artifacts suitable for Xenia process startup and later inspection
        min_lines: 110
      - path: src/features/library/components/ProfileEditorPanel.tsx
        provides: Standard labeled-field editor with explicit-versus-effective toggle, changed-field highlighting, and profile lifecycle actions inside the game detail domain
        min_lines: 120
      - path: src/features/library/components/ProfileRawEditor.tsx
        provides: Advanced raw key editor that reads and writes the same sparse explicit-value document as the standard profile editor
        min_lines: 90
      - path: src/features/library/state/libraryStore.ts
        provides: Renderer state for editor drafts, dirty-state tracking, unsaved-change protection, and launch-time profile summary refresh
        min_lines: 150
    key_links:
      - from: src-tauri/src/library/launch.rs
        to: src-tauri/src/profiles/materialize.rs
        via: Launch preflight and process spawn consume the same effective profile config that the user reviewed rather than recomputing or mutating global Xenia settings ad hoc
        pattern: launch|profile|effective|materialize
      - from: src/features/library/components/GameDetailsPanel.tsx
        to: src/features/library/components/ProfileEditorPanel.tsx
        via: Profile management stays inside the detail-first library workflow next to patch and launch controls
        pattern: profile|detail|edit|summary
      - from: src/features/library/components/ProfileEditorPanel.tsx
        to: src/features/library/components/ProfileRawEditor.tsx
        via: Standard and raw editors share one underlying explicit-value draft instead of drifting into separate sources of truth
        pattern: raw|field|draft|profile
      - from: src/features/library/components/LaunchPreflightPanel.tsx
        to: src/features/library/components/ProfileSummaryCard.tsx
        via: Pre-launch readiness surfaces show which profile and key changed settings will actually be used when the title starts
        pattern: launch|profile|changed|summary
---

<objective>
Build profile editor UI and wire launch-time config application.

Purpose: Turn the backend profile system into a usable game-detail workflow where users can inspect explicit versus effective settings, edit profiles safely, and launch a title with the selected patch and effective profile applied together.
Output: Profile summary and editor surfaces, raw editor, unsaved-change protection, renderer draft state, and launch-time profile materialization integrated with the existing library launch flow.
</objective>

<execution_context>
@/home/sergeng/.codex/get-shit-done/workflows/execute-plan.md
@/home/sergeng/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/06-profiles-and-community-settings/06-CONTEXT.md
@.planning/phases/06-profiles-and-community-settings/06-RESEARCH.md
@.planning/phases/06-profiles-and-community-settings/06-01-SUMMARY.md
@.planning/phases/06-profiles-and-community-settings/06-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add launch-time profile materialization and integrate it into the backend launch path</name>
  <files>src-tauri/src/profiles/materialize.rs, src-tauri/src/profiles/merge.rs, src-tauri/src/library/launch.rs, src-tauri/src/commands/profiles.rs, src-tauri/src/commands/library.rs, src-tauri/src/lib.rs</files>
  <action>Create a backend materialization path that takes the selected game's active profile, computes the effective config, combines it with the active patch state from Phase 5, and prepares derived launch-time config artifacts before Xenia is spawned. The launch flow must use the same effective settings shown in the UI and avoid mutating a user's long-lived global Xenia config file unexpectedly.</action>
  <verify><automated>cargo test --manifest-path src-tauri/Cargo.toml library::launch profiles::materialize</automated></verify>
  <done>The backend can explain and materialize the exact effective profile config used for a launch, and the launch path applies patch plus profile state together deterministically.</done>
</task>

<task type="auto">
  <name>Task 2: Build the profile summary, standard editor, raw editor, and unsaved-change flow</name>
  <files>src/features/library/components/ProfileSummaryCard.tsx, src/features/library/components/ProfileEditorPanel.tsx, src/features/library/components/ProfileRawEditor.tsx, src/features/library/components/UnsavedProfileChangesDialog.tsx</files>
  <action>Build the game-detail profile surfaces so users can see the active profile name, key changed settings, explicit-versus-effective toggle, and changed-field highlighting. The standard editor should use labeled fields, the raw editor should edit the same underlying explicit-value draft, and leaving the editor with unsaved changes should trigger a warning flow instead of silently discarding edits.</action>
  <verify><automated>npm run test -- --run src/features/library/components</automated></verify>
  <done>Users can inspect effective settings, edit profiles through both guided and raw modes, and receive a clear warning before losing unsaved profile edits.</done>
</task>

<task type="auto">
  <name>Task 3: Wire profile editing and launch summaries into the game detail experience</name>
  <files>src/features/library/api/libraryClient.ts, src/features/library/model/profileTypes.ts, src/features/library/state/libraryStore.ts, src/features/library/components/GameDetailsPanel.tsx, src/features/library/components/LaunchPreflightPanel.tsx</files>
  <action>Extend the library detail state and UI so profile drafts, dirty-state tracking, active-profile summaries, and launch preflight presentation stay coherent in one workflow. Before launch, the detail view should show the active profile name plus key changed settings, and the launch readiness surface should confirm that the selected patch and effective profile will be applied together.</action>
  <verify><automated>npm run test -- --run src/features/library/state src/features/library</automated></verify>
  <done>The detail-first library workflow can edit profiles safely, surface the resulting effective settings clearly, and show exactly what profile changes will apply when the title launches.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] cargo test --manifest-path src-tauri/Cargo.toml library::launch profiles::materialize
- [ ] npm run test -- --run src/features/library/components
- [ ] npm run test -- --run src/features/library/state src/features/library
- [ ] npm run build
</verification>

<success_criteria>

- All tasks completed
- The game detail workflow shows active profile summary plus explicit-versus-effective config views
- Standard and raw editors stay in sync and protect against losing unsaved changes
- Launch applies the selected patch and effective profile together using backend materialized config state

</success_criteria>

<output>
After completion, create `.planning/phases/06-profiles-and-community-settings/06-03-SUMMARY.md`
</output>
