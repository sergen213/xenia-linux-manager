---
phase: 06-profiles-and-community-settings
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src-tauri/src/profiles/mod.rs
  - src-tauri/src/profiles/storage.rs
  - src-tauri/src/profiles/merge.rs
  - src-tauri/src/commands/profiles.rs
  - src-tauri/src/lib.rs
  - src/features/library/api/libraryClient.ts
  - src/features/library/model/profileTypes.ts
  - src/features/library/state/libraryStore.ts
autonomous: true
requirements: [PROF-01, PROF-03, PROF-04]
user_setup: []
must_haves:
    truths:
      - Each game can own multiple named local profiles, profile names are unique within that game, and one active profile selection is persisted independently from profile contents
      - Profiles store only explicit values while the backend computes the full effective config by merging defaults and explicit overrides, so removing a field restores inheritance instead of persisting an empty value
      - The backend exposes both explicit-value and effective-config views, including changed-field metadata, so later editor and launch workflows do not need to reimplement merge rules in the renderer
    artifacts:
      - path: src-tauri/src/profiles/storage.rs
        provides: Backend-owned per-game profile manifest, profile document persistence, unique-name enforcement, and active-profile selection rooted in library metadata
        min_lines: 130
      - path: src-tauri/src/profiles/merge.rs
        provides: Sparse-override merge engine that computes explicit versus effective config views and changed-field summaries from Xenia defaults plus profile overrides
        min_lines: 120
      - path: src-tauri/src/commands/profiles.rs
        provides: Typed Tauri commands for listing, creating, renaming, deleting, selecting, and resolving effective profile payloads
        min_lines: 90
      - path: src/features/library/state/libraryStore.ts
        provides: Renderer state and actions for per-game profile inventory, active-profile selection, and effective-config loading inside the library detail workflow
        min_lines: 120
    key_links:
      - from: src-tauri/src/commands/profiles.rs
        to: src-tauri/src/profiles/storage.rs
        via: Renderer-facing profile commands persist canonical profile inventory and active-selection state in backend-owned metadata
        pattern: profile|active|rename|select
      - from: src-tauri/src/profiles/merge.rs
        to: src-tauri/src/profiles/storage.rs
        via: Stored explicit profile overrides are transformed into effective config views without mutating the canonical saved profile document
        pattern: effective|explicit|merge|changed
      - from: src/features/library/api/libraryClient.ts
        to: src-tauri/src/commands/profiles.rs
        via: The library feature requests typed profile payloads and effective-config views instead of reconstructing merge logic in the renderer
        pattern: profile|effective|explicit|select
      - from: src/features/library/state/libraryStore.ts
        to: src/features/library/model/profileTypes.ts
        via: Profile inventory, active selection, and effective-setting summaries are modeled explicitly in the library domain
        pattern: profile|summary|changed|effective
---

<objective>
Implement profile storage, merge rules, and effective-settings computation.

Purpose: Create the backend contract for per-game Xenia profiles so the manager can persist multiple local profiles, resolve one active profile deterministically, and explain both explicit profile values and the full effective config before any editor or launch-time UI is built.
Output: Backend profile storage and merge modules, typed profile commands, and renderer state hooks for profile inventory, selection, and effective-settings loading.
</objective>

<execution_context>
@/home/sergeng/.codex/get-shit-done/workflows/execute-plan.md
@/home/sergeng/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/06-profiles-and-community-settings/06-CONTEXT.md
@.planning/phases/06-profiles-and-community-settings/06-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build the backend profile inventory and per-game selection model</name>
  <files>src-tauri/src/profiles/mod.rs, src-tauri/src/profiles/storage.rs, src-tauri/src/commands/profiles.rs, src-tauri/src/lib.rs</files>
  <action>Create a backend-owned profile domain under `library_metadata_path` that stores multiple named profiles per canonical game, enforces per-game unique names, persists the active-profile reference separately from the saved profile contents, and supports blank-profile creation when a game has no prior profile. The storage model must stay local-first and avoid any dependency on mutable Xenia install files.</action>
  <verify><automated>cargo test --manifest-path src-tauri/Cargo.toml profiles::storage</automated></verify>
  <done>The backend can persist per-game profile inventory, enforce name uniqueness, and reload the active-profile selection without renderer-only assumptions.</done>
</task>

<task type="auto">
  <name>Task 2: Implement sparse override merge logic for explicit and effective config views</name>
  <files>src-tauri/src/profiles/merge.rs, src-tauri/src/profiles/storage.rs, src-tauri/src/commands/profiles.rs</files>
  <action>Implement a merge engine that treats saved profiles as sparse explicit overrides against the Xenia default config model, computes the full effective config on demand, and returns changed-field metadata for highlighting. Removing a value from a profile must restore inherited behavior instead of storing an empty override, and local profile values must win over any lower-precedence defaults.</action>
  <verify><automated>cargo test --manifest-path src-tauri/Cargo.toml profiles::merge</automated></verify>
  <done>The backend can return both explicit-value and effective-config payloads for a profile, with deterministic precedence and changed-field summaries.</done>
</task>

<task type="auto">
  <name>Task 3: Add typed renderer contracts for profile inventory, selection, and effective-setting summaries</name>
  <files>src/features/library/api/libraryClient.ts, src/features/library/model/profileTypes.ts, src/features/library/state/libraryStore.ts</files>
  <action>Extend the library feature's client and reducer state so profile inventory, active-profile selection, profile creation and rename actions, and effective-setting summaries stay in the same detail-first workflow established by the library domain. The renderer should consume typed explicit and effective profile payloads from the backend rather than owning profile merge behavior itself.</action>
  <verify><automated>npm run test -- --run src/features/library/state src/features/library/api</automated></verify>
  <done>The renderer can load profile inventory, switch the active profile for a selected game, and display profile summary data through typed backend contracts.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] cargo test --manifest-path src-tauri/Cargo.toml profiles::storage
- [ ] cargo test --manifest-path src-tauri/Cargo.toml profiles::merge
- [ ] npm run test -- --run src/features/library/state src/features/library/api
- [ ] npm run build
</verification>

<success_criteria>

- All tasks completed
- Each game can keep multiple named local profiles with one persisted active selection
- Effective settings are computed from sparse explicit overrides instead of mutating a global config file directly
- The renderer can request both explicit and effective profile payloads through typed backend contracts

</success_criteria>

<output>
After completion, create `.planning/phases/06-profiles-and-community-settings/06-01-SUMMARY.md`
</output>
