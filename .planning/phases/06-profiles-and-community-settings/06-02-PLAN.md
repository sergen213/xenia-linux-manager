---
phase: 06-profiles-and-community-settings
plan: 02
type: execute
wave: 2
depends_on: [01]
files_modified:
  - src-tauri/src/profiles/storage.rs
  - src-tauri/src/profiles/sources.rs
  - src-tauri/src/profiles/merge.rs
  - src-tauri/src/commands/profiles.rs
  - src-tauri/src/lib.rs
  - src/features/library/api/libraryClient.ts
  - src/features/library/model/profileTypes.ts
  - src/features/library/state/libraryStore.ts
  - src/features/library/components/GameDetailsPanel.tsx
autonomous: true
requirements: [PROF-02, PROF-04]
user_setup: []
must_haves:
    truths:
      - Profile metadata tracks provenance so local profiles remain first-class while the backend can distinguish future recommended or imported baseline settings without inventing a separate profile model
      - The app does not ship placeholder community tabs or dead-end affordances; any recommendation UI appears only when the backend can resolve an actual supported profile for the current title
      - Applying a supported recommended baseline normalizes into the same local profile system and still preserves the rule that local edits override lower-precedence defaults
    artifacts:
      - path: src-tauri/src/profiles/sources.rs
        provides: Provenance-aware profile-source and recommendation-resolution adapters with explicit unsupported-state handling when no real recommended profile exists
        min_lines: 110
      - path: src-tauri/src/profiles/storage.rs
        provides: Extended profile manifest and metadata model that records source provenance, application timestamps, and recommendation linkage without splitting local and recommended profile storage
        min_lines: 140
      - path: src-tauri/src/commands/profiles.rs
        provides: Typed commands for recommendation lookup, apply-or-clone into local profile space, and unsupported-source reporting that the renderer can consume conditionally
        min_lines: 100
      - path: src/features/library/state/libraryStore.ts
        provides: Renderer state for conditional recommendation availability, provenance badges, and apply-recommended actions without adding placeholder UI
        min_lines: 130
    key_links:
      - from: src-tauri/src/profiles/sources.rs
        to: src-tauri/src/profiles/storage.rs
        via: Any supported recommended settings source normalizes into the canonical per-game profile model with provenance recorded instead of branching into a separate config path
        pattern: source|recommended|provenance|apply
      - from: src-tauri/src/commands/profiles.rs
        to: src-tauri/src/profiles/sources.rs
        via: Renderer-facing recommendation lookup and apply commands stay gated on actual source availability in the backend
        pattern: recommended|lookup|apply|unsupported
      - from: src/features/library/components/GameDetailsPanel.tsx
        to: src/features/library/state/libraryStore.ts
        via: Recommendation affordances are rendered only when supported for the selected title, avoiding any placeholder community UI in the default local-profile experience
        pattern: recommended|supported|profile|detail
      - from: src-tauri/src/profiles/merge.rs
        to: src-tauri/src/profiles/sources.rs
        via: Recommended baselines flow into the same precedence model so local edits continue to win over lower-precedence defaults
        pattern: baseline|override|effective|merge
---

<objective>
Add community-optimized settings ingestion with provenance tracking.

Purpose: Reconcile the roadmap requirement for recommended settings with the locked Phase 6 local-first direction by creating a provenance-aware source seam, conditional recommendation support, and normalization into the same local profile system without shipping placeholder community UI.
Output: Recommendation source adapters, provenance-aware profile metadata, typed recommendation commands, and conditional detail-view wiring that only appears when a supported recommendation exists.
</objective>

<execution_context>
@/home/sergeng/.codex/get-shit-done/workflows/execute-plan.md
@/home/sergeng/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/06-profiles-and-community-settings/06-CONTEXT.md
@.planning/phases/06-profiles-and-community-settings/06-RESEARCH.md
@.planning/phases/06-profiles-and-community-settings/06-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add a provenance-aware recommendation source contract without creating placeholder product UI</name>
  <files>src-tauri/src/profiles/sources.rs, src-tauri/src/profiles/storage.rs, src-tauri/src/profiles/merge.rs, src-tauri/src/commands/profiles.rs, src-tauri/src/lib.rs</files>
  <action>Implement a backend source layer that can resolve whether a selected game has a supported recommended-settings profile, normalize that source into the canonical profile model, and report an explicit unsupported state when no trustworthy recommendation exists. The source contract must preserve the current local-first product direction by avoiding assumptions that a live community catalog always exists.</action>
  <verify><automated>cargo test --manifest-path src-tauri/Cargo.toml profiles::sources</automated></verify>
  <done>The backend can answer `is a recommended profile available`, apply it into the local profile system when supported, and return a clean unsupported state otherwise.</done>
</task>

<task type="auto">
  <name>Task 2: Extend profile metadata and merge behavior for provenance and recommended baselines</name>
  <files>src-tauri/src/profiles/storage.rs, src-tauri/src/profiles/merge.rs, src-tauri/src/commands/profiles.rs</files>
  <action>Extend the profile manifest and effective-config merge engine so provenance, recommendation linkage, and apply timestamps are tracked alongside local profile edits without splitting profiles into incompatible storage families. If a supported recommendation is applied, it must become a normal local profile or baseline-backed profile entry whose later local edits still win over lower-precedence defaults.</action>
  <verify><automated>cargo test --manifest-path src-tauri/Cargo.toml profiles::merge profiles::sources</automated></verify>
  <done>The profile system can explain where a profile came from and still compute effective settings with the same override rules regardless of provenance.</done>
</task>

<task type="auto">
  <name>Task 3: Wire conditional recommendation availability and provenance indicators into the game detail workflow</name>
  <files>src/features/library/api/libraryClient.ts, src/features/library/model/profileTypes.ts, src/features/library/state/libraryStore.ts, src/features/library/components/GameDetailsPanel.tsx</files>
  <action>Extend the library detail workflow so it can load recommendation availability, show provenance indicators for profiles, and offer `apply recommended settings` only when the backend reports an actual supported recommendation for the selected title. Do not add empty community tabs, disabled placeholder buttons, or dead-end affordances when no source exists.</action>
  <verify><automated>npm run test -- --run src/features/library/state src/features/library/components</automated></verify>
  <done>The game detail flow remains local-profile centered by default, while supported titles can surface a real recommendation action with provenance-aware follow-through.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] cargo test --manifest-path src-tauri/Cargo.toml profiles::sources
- [ ] cargo test --manifest-path src-tauri/Cargo.toml profiles::merge profiles::sources
- [ ] npm run test -- --run src/features/library/state src/features/library/components
- [ ] npm run build
</verification>

<success_criteria>

- All tasks completed
- Recommendation support is provenance-aware and normalized into the same canonical profile model as local profiles
- No placeholder community UI appears when no supported recommendation exists
- Local profile edits continue to win over any lower-precedence recommended defaults

</success_criteria>

<output>
After completion, create `.planning/phases/06-profiles-and-community-settings/06-02-SUMMARY.md`
</output>
