---
phase: 02-xenia-installation-lifecycle
plan: 03
type: execute
wave: 3
depends_on: [01, 02]
files_modified:
  - src/features/xenia/api/xeniaClient.ts
  - src/features/xenia/model/xeniaTypes.ts
  - src/features/xenia/state/xeniaStore.ts
  - src/features/xenia/state/XeniaProvider.tsx
  - src/features/xenia/components/XeniaLifecycleCard.tsx
  - src/features/xenia/components/XeniaLifecycleDialog.tsx
  - src/features/xenia/components/XeniaRecoveryActions.tsx
  - src/features/dashboard/DashboardHome.tsx
  - src/features/tasks/TasksPage.tsx
  - src/App.tsx
autonomous: true
requirements: [XEN-02, XEN-03, XEN-04]
user_setup: []
must_haves:
    truths:
      - The main dashboard shows a primary adaptive Xenia action that switches correctly between Install, Update, and Retry based on backend lifecycle state
      - Users can inspect full install details, see update availability, confirm install/update actions, and continue navigating while work runs in the background
      - Failed lifecycle operations surface a friendly summary, technical details, and manual recovery actions including viewing logs, cleaning failed state, and removing the current install
    artifacts:
      - path: src/features/xenia/state/xeniaStore.ts
        provides: Renderer lifecycle state and adaptive-action derivation
        min_lines: 60
      - path: src/features/xenia/components/XeniaLifecycleCard.tsx
        provides: Dashboard install/update/retry surface with install details
        min_lines: 80
      - path: src/features/xenia/components/XeniaLifecycleDialog.tsx
        provides: Confirmation and success/failure dialog flow for lifecycle actions
        min_lines: 70
      - path: src/features/tasks/TasksPage.tsx
        provides: Recovery-aware task/log integration for Xenia lifecycle jobs
        min_lines: 70
    key_links:
      - from: src/features/xenia/api/xeniaClient.ts
        to: src-tauri/src/commands/xenia.rs
        via: Renderer invokes backend lifecycle commands for status, update checks, install, retry, cleanup, and removal
        pattern: invoke
      - from: src/features/xenia/state/xeniaStore.ts
        to: src/features/xenia/components/XeniaLifecycleCard.tsx
        via: Adaptive lifecycle state drives Install|Update|Retry button labeling and details
        pattern: install|update|retry
      - from: src/features/xenia/components/XeniaRecoveryActions.tsx
        to: src/features/tasks/TasksPage.tsx
        via: Failure UI routes users to logs and recovery handling backed by persisted task history
        pattern: logs|retry|cleanup
---

<objective>
Add the install/update UI, progress reporting, and recovery handling for the Xenia lifecycle.

Purpose: Fulfill the user-facing phase goal by turning the backend lifecycle contracts into a dashboard-first experience with adaptive actions, full install details, progress visibility, and failure recovery.
Output: Renderer Xenia client/store, dashboard lifecycle card, confirmation flow, success checklist, failure/recovery surfaces, and Tasks integration for logs and retry-safe messaging.
</objective>

<execution_context>
@/home/sergeng/.codex/get-shit-done/workflows/execute-plan.md
@/home/sergeng/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-xenia-installation-lifecycle/02-CONTEXT.md
@.planning/phases/02-xenia-installation-lifecycle/02-RESEARCH.md
@.planning/phases/02-xenia-installation-lifecycle/02-01-SUMMARY.md
@.planning/phases/02-xenia-installation-lifecycle/02-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build the renderer Xenia lifecycle client and adaptive state model</name>
  <files>src/features/xenia/api/xeniaClient.ts, src/features/xenia/model/xeniaTypes.ts, src/features/xenia/state/xeniaStore.ts, src/features/xenia/state/XeniaProvider.tsx, src/App.tsx</files>
  <action>Create a dedicated renderer lifecycle module that loads installed status on startup, performs automatic and manual update checks, derives the adaptive primary action label, and keeps backend-owned failure/retry state intact. Follow the existing context + reducer provider pattern used by settings and tasks rather than introducing a new state library.</action>
  <verify><automated>npm run test -- --run src/features/xenia/state</automated></verify>
  <done>The renderer can load backend lifecycle state, derive the correct Install|Update|Retry action, and provide shared Xenia lifecycle context to dashboard and recovery surfaces.</done>
</task>

<task type="auto">
  <name>Task 2: Add the dashboard-first install/update surface with confirmation and next-step flows</name>
  <files>src/features/xenia/components/XeniaLifecycleCard.tsx, src/features/xenia/components/XeniaLifecycleDialog.tsx, src/features/dashboard/DashboardHome.tsx</files>
  <action>Replace the dashboard's placeholder Xenia card with the real lifecycle card that shows installed status, exact version, release date, `Check for updates`, and the adaptive primary action. Add a confirmation dialog so install/update never starts silently, and after success show a next-step checklist with quick actions for library setup, settings review, and install details/logs.</action>
  <verify><automated>npm run test -- --run src/features/xenia/components src/features/dashboard</automated></verify>
  <done>The dashboard becomes the primary install/update control surface and presents the confirmation, progress, and next-step states required by the phase context.</done>
</task>

<task type="auto">
  <name>Task 3: Surface failure details, logs, cleanup, and retry-safe recovery handling</name>
  <files>src/features/xenia/components/XeniaRecoveryActions.tsx, src/features/xenia/components/XeniaLifecycleDialog.tsx, src/features/tasks/TasksPage.tsx</files>
  <action>Implement failure and recovery UI that pairs a friendly summary with technical details, links directly into job logs, and exposes manual recovery actions for cleanup and install removal. Preserve the generic background-task behavior from Phase 1, but wire Xenia lifecycle jobs into the recovery affordances so `Retry` respects whether the last failure came from install or update mode.</action>
  <verify><automated>npm run test -- --run src/features/xenia/components src/features/tasks</automated></verify>
  <done>Users can recover from failed lifecycle operations using visible logs, explicit cleanup/remove actions, and a Retry path that maps back to the correct backend lifecycle mode while the rest of the app remains navigable.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] npm run test -- --run src/features/xenia/state
- [ ] npm run test -- --run src/features/xenia/components src/features/dashboard
- [ ] npm run test -- --run src/features/xenia/components src/features/tasks
- [ ] npm run build
</verification>

<success_criteria>

- All tasks completed
- The dashboard exposes the primary adaptive Install|Update|Retry control with full install details
- Install and update flows require confirmation, show progress while background work continues, and end with next-step guidance
- Failure states expose both technical detail and manual recovery actions without leaving the emulator in invalid state

</success_criteria>

<output>
After completion, create `.planning/phases/02-xenia-installation-lifecycle/02-03-SUMMARY.md`
</output>
