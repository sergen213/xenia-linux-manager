---
phase: 02-xenia-installation-lifecycle
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src-tauri/src/xenia/mod.rs
  - src-tauri/src/xenia/releases.rs
  - src-tauri/src/xenia/download.rs
  - src-tauri/src/xenia/archive.rs
  - src-tauri/src/commands/xenia.rs
  - src-tauri/src/commands/mod.rs
  - src-tauri/src/lib.rs
autonomous: true
requirements: [XEN-02]
user_setup: []
must_haves:
    truths:
      - The app can discover the latest Linux Xenia Canary release metadata without relying on a hardcoded wiki download link
      - The app can download the selected Linux release archive into managed app storage and extract it into a validated staging directory
      - Starting an install job produces backend progress/log events that later UI plans can render directly
    artifacts:
      - path: src-tauri/src/xenia/releases.rs
        provides: Linux release metadata fetch and asset selection contract
        min_lines: 80
      - path: src-tauri/src/xenia/download.rs
        provides: Managed archive download pipeline for Xenia releases
        min_lines: 60
      - path: src-tauri/src/xenia/archive.rs
        provides: Archive extraction and extracted-layout validation
        min_lines: 70
      - path: src-tauri/src/commands/xenia.rs
        provides: Tauri install command surface for the renderer
        min_lines: 50
    key_links:
      - from: src-tauri/src/commands/xenia.rs
        to: src-tauri/src/xenia/releases.rs
        via: Install command resolves Linux release metadata before download
        pattern: latest|release|asset
      - from: src-tauri/src/commands/xenia.rs
        to: src-tauri/src/xenia/download.rs
        via: Install command streams archive download into staging storage
        pattern: download
      - from: src-tauri/src/xenia/download.rs
        to: src-tauri/src/xenia/archive.rs
        via: Downloaded archive is extracted and validated before any promotion step
        pattern: extract|validate
---

<objective>
Build the release metadata fetch, download, and archive extraction pipeline for Linux Xenia Canary installs.

Purpose: Establish the backend install primitives Phase 2 depends on before version tracking and UI work are layered on top.
Output: Rust release client, Linux asset selection, managed archive download, extracted-layout validation, and Tauri commands that launch install jobs through the existing task infrastructure.
</objective>

<execution_context>
@/home/sergeng/.codex/get-shit-done/workflows/execute-plan.md
@/home/sergeng/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-xenia-installation-lifecycle/02-CONTEXT.md
@.planning/phases/02-xenia-installation-lifecycle/02-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement the Rust release client and Linux asset selection contract</name>
  <files>src-tauri/src/xenia/mod.rs, src-tauri/src/xenia/releases.rs, src-tauri/src/commands/xenia.rs, src-tauri/src/commands/mod.rs, src-tauri/src/lib.rs</files>
  <action>Create a dedicated `xenia` backend module that fetches release metadata for `xenia-canary/xenia-canary-releases`, normalizes the latest Linux-compatible asset into a typed release record, and exposes Tauri commands the renderer can call later. Keep URL discovery metadata-driven instead of hardcoding a wiki asset URL so stale documentation cannot break installs silently.</action>
  <verify><automated>cargo test --manifest-path src-tauri/Cargo.toml xenia::releases</automated></verify>
  <done>The backend can resolve the latest Linux Xenia release metadata into a typed record with release tag, publish date, Linux asset name, and download URL suitable for the install pipeline.</done>
</task>

<task type="auto">
  <name>Task 2: Build managed archive download and extracted-layout validation</name>
  <files>src-tauri/src/xenia/download.rs, src-tauri/src/xenia/archive.rs, src-tauri/src/xenia/mod.rs</files>
  <action>Implement the release archive download flow into an app-data staging area, then extract into a release-specific temporary directory and validate the expected emulator layout before anything is considered installable. Do not write directly into the active Xenia directory in this plan; the output of this work is a validated staged candidate build, not a promoted install.</action>
  <verify><automated>cargo test --manifest-path src-tauri/Cargo.toml xenia::archive</automated></verify>
  <done>A Linux release archive can be downloaded into managed storage, extracted into a staging directory, and rejected early if the resulting layout is incomplete or unusable.</done>
</task>

<task type="auto">
  <name>Task 3: Launch install jobs through the existing task/progress infrastructure</name>
  <files>src-tauri/src/commands/xenia.rs, src-tauri/src/xenia/download.rs, src-tauri/src/xenia/archive.rs</files>
  <action>Wire the install entrypoint through the shared job subsystem so download and extraction steps emit job creation, progress, and log events. Structure the command for a first install only in this plan, but keep the command signature and job labels generic enough that Plan 02 can reuse the same pipeline for update attempts.</action>
  <verify><automated>cargo test --manifest-path src-tauri/Cargo.toml xenia::install</automated></verify>
  <done>Starting an install produces a background job with step-by-step logs and progress updates while the staged candidate build is prepared in backend-managed storage.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] cargo test --manifest-path src-tauri/Cargo.toml xenia::releases
- [ ] cargo test --manifest-path src-tauri/Cargo.toml xenia::archive
- [ ] cargo test --manifest-path src-tauri/Cargo.toml xenia::install
</verification>

<success_criteria>

- All tasks completed
- Linux Xenia release discovery is metadata-driven and does not depend on a stale hardcoded download URL
- The backend can download and extract a Linux release into a validated staging directory
- Install work emits job progress/log data through the existing task infrastructure

</success_criteria>

<output>
After completion, create `.planning/phases/02-xenia-installation-lifecycle/02-01-SUMMARY.md`
</output>
