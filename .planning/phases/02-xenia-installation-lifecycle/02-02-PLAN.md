---
phase: 02-xenia-installation-lifecycle
plan: 02
type: execute
wave: 2
depends_on: [01]
files_modified:
  - src-tauri/src/xenia/install_state.rs
  - src-tauri/src/xenia/lifecycle.rs
  - src-tauri/src/xenia/releases.rs
  - src-tauri/src/xenia/mod.rs
  - src-tauri/src/commands/xenia.rs
autonomous: true
requirements: [XEN-03, XEN-04]
user_setup: []
must_haves:
    truths:
      - The app can report the currently installed Xenia version, release date, and active executable path from persisted install state
      - The app can determine when a newer Linux release is available without guessing from directory names alone
      - Failed or interrupted install and update attempts keep the previous working install active and preserve enough lifecycle context for a correct Retry action
    artifacts:
      - path: src-tauri/src/xenia/install_state.rs
        provides: Persisted install manifest and lifecycle-state model
        min_lines: 80
      - path: src-tauri/src/xenia/lifecycle.rs
        provides: Promotion, rollback-safe update handling, and retry context
        min_lines: 90
      - path: src-tauri/src/commands/xenia.rs
        provides: Commands for status, update checks, retry-safe install/update execution, and cleanup hooks
        min_lines: 70
    key_links:
      - from: src-tauri/src/commands/xenia.rs
        to: src-tauri/src/xenia/install_state.rs
        via: Status and update-check commands expose persisted install details to the renderer
        pattern: status|installed|version
      - from: src-tauri/src/xenia/lifecycle.rs
        to: src-tauri/src/xenia/releases.rs
        via: Update logic compares installed build metadata against latest Linux release metadata
        pattern: latest|update
      - from: src-tauri/src/xenia/lifecycle.rs
        to: src-tauri/src/xenia/install_state.rs
        via: Promotion records active build details and failed-operation retry context
        pattern: manifest|state|retry
---

<objective>
Implement installed-build tracking, update checks, and retry-safe promotion for the Xenia lifecycle.

Purpose: Turn the staged install primitives from Plan 01 into a real install/update system that can keep one working build active while handling retries and failures safely.
Output: Persisted install manifest, lifecycle-state model, update detection, promotion/rollback handling, and backend commands for installed status, update checks, retry, cleanup, and removal.
</objective>

<execution_context>
@/home/sergeng/.codex/get-shit-done/workflows/execute-plan.md
@/home/sergeng/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-xenia-installation-lifecycle/02-CONTEXT.md
@.planning/phases/02-xenia-installation-lifecycle/02-RESEARCH.md
@.planning/phases/02-xenia-installation-lifecycle/02-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Persist install manifests and lifecycle state for installed and failed operations</name>
  <files>src-tauri/src/xenia/install_state.rs, src-tauri/src/xenia/mod.rs, src-tauri/src/commands/xenia.rs</files>
  <action>Create the backend install-state document that records active install details, last successful release info, last failed operation metadata, and retry mode (`install` vs `update`). Store this under app-managed data so the UI can load installed version, release date, and failure context directly on startup without reconstructing state from filesystem guesses.</action>
  <verify><automated>cargo test --manifest-path src-tauri/Cargo.toml xenia::install_state</automated></verify>
  <done>The backend can persist and reload installed-build details plus failure/retry context needed for accurate status and retry behavior across app restarts.</done>
</task>

<task type="auto">
  <name>Task 2: Implement promotion, update comparison, and rollback-safe failure handling</name>
  <files>src-tauri/src/xenia/lifecycle.rs, src-tauri/src/xenia/releases.rs, src-tauri/src/xenia/mod.rs</files>
  <action>Use the staged candidate build from Plan 01 to implement promotion into the managed Xenia install directory, update checks against the latest Linux release, and rollback-safe failure behavior. Preserve the currently active install until the candidate build has been fully validated and promoted, and ensure failed updates leave the previous working build active instead of partially replaced.</action>
  <verify><automated>cargo test --manifest-path src-tauri/Cargo.toml xenia::lifecycle</automated></verify>
  <done>The backend can compare installed vs latest release metadata, promote a staged build into the active install safely, and recover from failed updates without invalidating the previously working build.</done>
</task>

<task type="auto">
  <name>Task 3: Expose installed status, manual update checks, retry, cleanup, and remove-install commands</name>
  <files>src-tauri/src/commands/xenia.rs, src-tauri/src/xenia/install_state.rs, src-tauri/src/xenia/lifecycle.rs</files>
  <action>Expand the Tauri command surface so the renderer can request install status, check for updates, start an update job, retry the last failed lifecycle mode, inspect cleanup options, and remove the current install. Keep these commands backend-owned so the dashboard later renders the true lifecycle state rather than inventing one in React state.</action>
  <verify><automated>cargo test --manifest-path src-tauri/Cargo.toml xenia::commands</automated></verify>
  <done>The backend exposes one consistent Xenia lifecycle API for installed status, update availability, retry-safe execution, cleanup, and install removal.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] cargo test --manifest-path src-tauri/Cargo.toml xenia::install_state
- [ ] cargo test --manifest-path src-tauri/Cargo.toml xenia::lifecycle
- [ ] cargo test --manifest-path src-tauri/Cargo.toml xenia::commands
</verification>

<success_criteria>

- All tasks completed
- Installed Xenia version and release details can be loaded from persisted state
- Update availability is derived from release metadata comparison
- Failed install/update attempts preserve retry context and never leave the active install in an invalid state

</success_criteria>

<output>
After completion, create `.planning/phases/02-xenia-installation-lifecycle/02-02-SUMMARY.md`
</output>
