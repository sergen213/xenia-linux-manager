---
phase: 08-packaging-and-release-hardening
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src-tauri/Cargo.toml
  - src-tauri/tauri.conf.json
  - src-tauri/capabilities/default.json
  - src-tauri/src/release/mod.rs
  - src-tauri/src/commands/release.rs
  - src-tauri/src/lib.rs
  - src/features/settings/api/releaseClient.ts
  - src/features/settings/model/releaseTypes.ts
  - src/features/settings/state/settingsStore.ts
  - package.json
  - scripts/generate-updater-manifest.mjs
  - .github/workflows/release-appimage.yml
autonomous: true
requirements: [APP-02]
user_setup: []
must_haves:
    truths:
      - The project produces a deliberate Linux AppImage release artifact with correct desktop metadata, icons, bundled assets, and Linux-specific AppImage file placement instead of relying on default Tauri bundling alone
      - Packaged builds expose a backend-owned release metadata contract that tells the renderer whether it is running as a packaged AppImage, what version and build it is, where release notes live, and whether updater support is actually available
      - In-app update handling is confirmation-gated and only enabled when the Tauri v2 updater path is fully wired with signed artifacts, configured endpoints, and frontend plus backend plugin registration
    artifacts:
      - path: src-tauri/tauri.conf.json
        provides: Linux-specific AppImage bundle configuration, updater artifact generation, desktop metadata, and packaged release settings for the shipped app
        min_lines: 55
      - path: src-tauri/src/release/mod.rs
        provides: Runtime release metadata, packaged-environment classification, desktop integration state helpers, and typed updater capability reporting for the renderer
        min_lines: 150
      - path: scripts/generate-updater-manifest.mjs
        provides: Repeatable generation of signed updater manifest data from AppImage build outputs so release publishing does not depend on hand-edited metadata
        min_lines: 120
      - path: .github/workflows/release-appimage.yml
        provides: Repeatable AppImage build, signing, and release-artifact assembly workflow for v1 Linux distribution
        min_lines: 80
    key_links:
      - from: src-tauri/src/lib.rs
        to: src-tauri/src/release/mod.rs
        via: The app registers release metadata and updater capabilities through one backend module so packaged-only behavior does not leak across unrelated feature areas
        pattern: release|updater|packaged|appimage
      - from: src/features/settings/api/releaseClient.ts
        to: src-tauri/src/commands/release.rs
        via: Renderer-facing release and updater state is fetched through typed Tauri commands instead of ad hoc environment probing in React components
        pattern: release|build|version|notes
      - from: src-tauri/tauri.conf.json
        to: .github/workflows/release-appimage.yml
        via: Release automation consumes the same AppImage and updater artifact contract that the local package build uses, keeping CI and local release outputs aligned
        pattern: appimage|bundle|updater|artifact
      - from: scripts/generate-updater-manifest.mjs
        to: src-tauri/tauri.conf.json
        via: Manifest generation depends on the configured updater artifact format and Linux AppImage bundle output rather than a separate release-only convention
        pattern: latest|manifest|sig|appimage
---

<objective>
Create the shipped AppImage packaging and release automation contract.

Purpose: Turn the current default Tauri bundle setup into a real Linux release pipeline with AppImage-first metadata, packaged runtime/build introspection, signed updater artifacts, and repeatable release assembly that Phase 8 verification can exercise.
Output: Hardened Tauri packaging config, release metadata commands, typed renderer release client, updater-capable release plumbing, manifest generation script, and AppImage release workflow automation.
</objective>

<execution_context>
@/home/sergeng/.codex/get-shit-done/workflows/execute-plan.md
@/home/sergeng/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/08-packaging-and-release-hardening/08-CONTEXT.md
@.planning/phases/08-packaging-and-release-hardening/08-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Harden Tauri AppImage bundle configuration and capability scope for packaged release builds</name>
  <files>src-tauri/Cargo.toml, src-tauri/tauri.conf.json, src-tauri/capabilities/default.json, package.json</files>
  <action>Update the Tauri desktop configuration so Linux AppImage is the deliberate v1 release target, with explicit product naming, bundle metadata, icons, AppImage file injection where required, and updater-artifact generation enabled only through a coherent signed-release path. Add any Rust and frontend plugin dependencies needed for updater or restart flows, and expand capability permissions only to the minimum required for release notes opening, updater operation, and packaged desktop behavior.</action>
  <verify><automated>npm run tauri build -- --bundles appimage --config src-tauri/tauri.conf.json</automated></verify>
  <done>The repo can produce a real AppImage bundle with the intended Linux metadata and the minimum permissions needed for packaged release behavior.</done>
</task>

<task type="auto">
  <name>Task 2: Add backend release metadata and updater capability commands for packaged builds</name>
  <files>src-tauri/src/release/mod.rs, src-tauri/src/commands/release.rs, src-tauri/src/lib.rs, src/features/settings/model/releaseTypes.ts, src/features/settings/api/releaseClient.ts, src/features/settings/state/settingsStore.ts</files>
  <action>Create a backend-owned release module that reports version, build kind, architecture, release notes URL, updater availability, and packaged-environment diagnostics through typed commands. Wire this into the existing app state patterns so React surfaces can ask one consistent source whether they are in a packaged AppImage build, whether update checks are supported, and what warnings or metadata should be shown before later UI hardening work begins.</action>
  <verify><automated>cargo test --manifest-path src-tauri/Cargo.toml release</automated></verify>
  <done>The renderer can query one typed backend contract for packaged-build identity, updater readiness, and release metadata instead of inferring packaging state on its own.</done>
</task>

<task type="auto">
  <name>Task 3: Build repeatable AppImage release assembly and updater manifest generation</name>
  <files>scripts/generate-updater-manifest.mjs, .github/workflows/release-appimage.yml, package.json, src-tauri/tauri.conf.json</files>
  <action>Implement the release-automation path that builds the AppImage, collects generated signatures, emits a publishable updater manifest, and leaves behind a repeatable workflow for maintainers to assemble a v1 release. Keep the automation Linux-focused and AppImage-first, and make sure the generated metadata stays aligned with the runtime version and updater endpoint contract rather than requiring manual JSON editing.</action>
  <verify><automated>node scripts/generate-updater-manifest.mjs --help</automated></verify>
  <done>The project has a documented and executable path to build AppImage release artifacts, generate updater metadata, and hand off a coherent Linux release package for publication.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] cargo test --manifest-path src-tauri/Cargo.toml release
- [ ] npm run test -- --run src/features/settings/api src/features/settings/state
- [ ] npm run build
- [ ] npm run tauri build -- --bundles appimage
- [ ] node scripts/generate-updater-manifest.mjs --help
</verification>

<success_criteria>

- All tasks completed
- The repo produces a Linux AppImage with explicit release metadata and desktop bundle settings rather than the default generic Tauri output
- The app exposes typed packaged-build and updater-readiness metadata to the renderer
- Release automation can assemble AppImage updater artifacts and a publishable manifest without manual metadata editing

</success_criteria>

<output>
After completion, create `.planning/phases/08-packaging-and-release-hardening/08-01-SUMMARY.md`
</output>
