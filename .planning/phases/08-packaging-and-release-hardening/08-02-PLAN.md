---
phase: 08-packaging-and-release-hardening
plan: 02
type: execute
wave: 2
depends_on: [01]
files_modified:
  - src/components/app-shell/StatusBar.tsx
  - src/components/app-shell/StatusBar.css
  - src/features/settings/SettingsPage.tsx
  - src/features/settings/SettingsPage.css
  - src/features/settings/components/ReleaseChannelCard.tsx
  - src/features/settings/components/ReleaseChannelCard.css
  - src/features/settings/components/PackagedEnvironmentNotice.tsx
  - src/features/settings/components/PackagedEnvironmentNotice.css
  - src/features/xenia/components/XeniaLifecycleDialog.tsx
  - src/features/xenia/components/XeniaLifecycleDialog.css
  - docs/release/appimage-verification-checklist.md
  - scripts/verify-appimage-release.sh
autonomous: true
requirements: [APP-02]
user_setup: []
must_haves:
    truths:
      - The packaged build clearly identifies itself as `Xenia Manager for Linux`, shows version or build information plus release notes access, and explains packaged-environment warnings in plain language with technical details hidden behind disclosure by default
      - Desktop integration and update-related prompts are explicit, user-confirmed, and recoverable from the app later instead of being silent one-shot behaviors
      - Release readiness is backed by an internal packaged-form verification checklist that covers install or update, scan, launch, patch/profile, save, and desktop integration flows and blocks release when packaged regressions appear
    artifacts:
      - path: src/features/settings/components/ReleaseChannelCard.tsx
        provides: Packaged-build release metadata surface with version, build kind, release notes link, updater state, and explicit confirmation entry points
        min_lines: 110
      - path: src/features/settings/components/PackagedEnvironmentNotice.tsx
        provides: User-facing packaged-environment warning surface with plain-language summaries and expandable technical detail for Linux AppImage diagnostics
        min_lines: 120
      - path: docs/release/appimage-verification-checklist.md
        provides: Internal release-blocking checklist for packaged AppImage verification across all required core workflows and desktop integration behavior
        min_lines: 120
      - path: scripts/verify-appimage-release.sh
        provides: Repeatable local helper for smoke-checking the AppImage artifact and opening the internal checklist workflow for manual verification
        min_lines: 80
    key_links:
      - from: src/components/app-shell/StatusBar.tsx
        to: src/features/settings/components/ReleaseChannelCard.tsx
        via: The shell advertises live build and package state while deeper release and updater controls live in Settings, avoiding duplicate release metadata logic
        pattern: version|build|release|update
      - from: src/features/settings/components/PackagedEnvironmentNotice.tsx
        to: src/features/xenia/components/XeniaLifecycleDialog.tsx
        via: Risky packaged-environment and update warnings use the same plain-language plus expandable-detail posture across settings and lifecycle confirmation flows
        pattern: warning|details|update|environment
      - from: docs/release/appimage-verification-checklist.md
        to: scripts/verify-appimage-release.sh
        via: The local verification helper points maintainers at the release-blocking checklist so packaged smoke testing remains tied to the documented manual evidence path
        pattern: verify|checklist|appimage|release
      - from: src/features/settings/SettingsPage.tsx
        to: src/features/settings/components/PackagedEnvironmentNotice.tsx
        via: Packaged-release diagnostics and desktop integration recovery belong in Settings where users can revisit them after first launch
        pattern: desktop|integration|package|settings
---

<objective>
Run the packaged-app verification and release hardening pass.

Purpose: Turn the packaging plumbing from Plan 01 into a trustworthy packaged experience with Linux-native build visibility, explicit update and desktop-integration messaging, and a release-blocking verification process that validates the app as shipped.
Output: Packaged release UI surfaces, plain-language environment warnings with expandable technical detail, update confirmation messaging, internal AppImage verification checklist, and a local packaged-release smoke helper.
</objective>

<execution_context>
@/home/sergeng/.codex/get-shit-done/workflows/execute-plan.md
@/home/sergeng/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/08-packaging-and-release-hardening/08-CONTEXT.md
@.planning/phases/08-packaging-and-release-hardening/08-RESEARCH.md
@.planning/phases/08-packaging-and-release-hardening/08-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Surface packaged build metadata, release notes, and desktop integration state in the shell and settings</name>
  <files>src/components/app-shell/StatusBar.tsx, src/components/app-shell/StatusBar.css, src/features/settings/SettingsPage.tsx, src/features/settings/SettingsPage.css, src/features/settings/components/ReleaseChannelCard.tsx, src/features/settings/components/ReleaseChannelCard.css</files>
  <action>Replace the placeholder status items with packaged-build-aware status content and add a dedicated release section in Settings that shows the packaged app name, version, build kind, architecture, release notes link, updater state, and desktop integration recovery actions. The UI should make it obvious when the user is in a packaged AppImage build instead of a dev session while staying consistent with the project’s existing shell and settings patterns.</action>
  <verify><automated>npm run test -- --run src/components/app-shell src/features/settings</automated></verify>
  <done>The packaged build identifies itself clearly in the shell and settings surfaces and gives users a direct place to inspect release metadata, release notes, and desktop integration state.</done>
</task>

<task type="auto">
  <name>Task 2: Add plain-language packaged warnings and confirmation-gated update messaging</name>
  <files>src/features/settings/components/PackagedEnvironmentNotice.tsx, src/features/settings/components/PackagedEnvironmentNotice.css, src/features/xenia/components/XeniaLifecycleDialog.tsx, src/features/xenia/components/XeniaLifecycleDialog.css, src/features/settings/SettingsPage.tsx</files>
  <action>Implement packaged-environment notices that explain risky or unsupported AppImage conditions in plain language, hide technical details behind a disclosure by default, and reserve hard blocks only for truly unusable conditions. Extend lifecycle or update dialogs so packaged update handling is never silent: users should see what version is available, where release notes live, and that they are explicitly confirming the update before it proceeds.</action>
  <verify><automated>npm run test -- --run src/features/xenia/components src/features/settings/components</automated></verify>
  <done>Packaged warnings and update prompts match the context’s safety posture: approachable, explicit, recoverable, and technically detailed only when the user asks for more.</done>
</task>

<task type="auto">
  <name>Task 3: Create the internal AppImage verification checklist and packaged smoke helper</name>
  <files>docs/release/appimage-verification-checklist.md, scripts/verify-appimage-release.sh</files>
  <action>Write the internal-only packaged release checklist and a lightweight local helper that guides maintainers through packaged-form verification against the produced AppImage. The checklist must cover first launch, desktop integration, install or update, library scan, game launch, patch/profile behavior, save workflows, release metadata visibility, and update prompting, and it must make clear that any failure blocks release until fixed.</action>
  <verify><automated>bash scripts/verify-appimage-release.sh --help</automated></verify>
  <done>The project has a release-blocking packaged verification procedure that is easy to rerun locally and broad enough to catch regressions beyond simple startup.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] npm run test -- --run src/components/app-shell src/features/settings
- [ ] npm run test -- --run src/features/xenia/components src/features/settings/components
- [ ] npm run build
- [ ] npm run tauri build -- --bundles appimage
- [ ] bash scripts/verify-appimage-release.sh --help
- [ ] Manual AppImage verification completed against docs/release/appimage-verification-checklist.md
</verification>

<success_criteria>

- All tasks completed
- The packaged build surfaces version/build information, release notes access, and packaged-environment warnings in a Linux-native, plain-language way
- Desktop integration and update flows are explicit and confirmation-gated rather than silent or irreversible
- An internal checklist exists that verifies install, scan, launch, patch/profile, save, and desktop integration behavior in packaged form before release

</success_criteria>

<output>
After completion, create `.planning/phases/08-packaging-and-release-hardening/08-02-SUMMARY.md`
</output>
